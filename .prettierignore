node_modules
dist
build
coverage
.turbo
pnpm-lock.yaml

# Generated by `supabase gen types` — keep generator formatting, do not prettier-format
packages/supabase/src/types/database.types.ts

# Archived design handoffs and one-off analysis dumps: not code, two of them
# are not even valid syntax (docs/archive/design_handoff_myk9/colors_and_type_v2.css).
docs/archive
docs/design
openspec/changes/archive
myk9q-analysis.json
mockup-dashboard.html
# Multi-line SQL inside a Markdown table cell: Prettier is not idempotent on it
# (each pass indents the cell deeper), so the file is left as written.
docs/operations/go-live-runbook.md

# Astro build output (gitignored, but prettier --check . still walks it)
**/.astro/

# Astro components: prettier cannot parse them without prettier-plugin-astro,
# which is not installed. Three files today; add the plugin to format them.
**/*.astro
