# Linear Todo implementation checkpoint — 2026-09-05

Execution of [the saved plan](../plan-linear-todo-2026-09-05.md), authorized in a separate implementation task. Branch: `codex/linear-todo-2026-09-05`, base `879d4ec`; refreshed main `dcc6260be49cc2fac68ff4c3b0ae14feaef277d3`. Local changes are not yet committed, reviewed independently, deployed, or merged. All eight issues were moved to In Progress and individually updated under the owner's explicit Linear authorization. None meets its full closure gate yet.

| Slice / issue | Local result and verification | Remaining evidence gate |
| --- | --- | --- |
| A1 / MYK9-356 | Preserved shipped absent semantics; added client/completion and transactional real-SQL parity coverage plus a loopback-only four-function mutation driver in CI. Client/placement 72 tests and SQL harness 8 tests passed. Hosted definitions confirm all four absent predicates and migration 20260904160000 applied. | Disposable migrated SQL baseline and installed-mutation red/restored green run. Docker unavailable and local database refused connection; shared DB was read only. |
| B1 / MYK9-412 | Isolated shell fixture environment, owned temporary files, bounded diagnostic errors with original cause and failing command line. Explicit nonzero fixture was red before implementation. Production notifier action unchanged. Focused notifier tests passed; last-duplicate guard mutation failed as expected and was restored. Low-priority shuffled coverage run passed 8 tests, but its single-file command failed repository-wide coverage thresholds. | Six full shuffled suite passes. First full run produced no progress beyond RUN/seed for over 60 seconds and was stopped per repository policy. Original intermittent cause remains unconfirmed. |
| B2 / MYK9-405 | Provenance-aware version guard; 12 disposable-git tests passed. Reversing inherited acceptance, conflicting-ref rejection, and deployed-version rejection produced red tests; restored tests passed. | Real migration PR Quality Checks and isolated deliberate-collision CI evidence or explicit owned equivalent. |
| C1a / MYK9-407 | Added missing 24-hour cadence metadata; full and carry-forward ACL thresholds pinned to 48 hours. Missing-key tests red before correction, green after. Read-only deployed bundle comparison found exactly the one missing cadence entry. | Authorized cron-health-check deployment and fresh hosted snapshot/UI parity. |
| C1b / MYK9-409 | Owner-approved validated target union and shared link renderer across map, triage, health rows and dashboard. Exact href, new-tab indication, rel, invalid targets and internal router continuity tested. Actual installed Router Link baseline already preserved absolute URLs; no fabricated malformed-/https regression claim. Reversing mapping and renderer properties failed consumer tests. | Independent review, PR/CI and merge. |
| D1 / MYK9-358 | Two-line provenance header corrected. Executable body byte-identical to 20260902170000: SHA-256 3b6344da9477d4b0a919d65a34bba773286b432f4d5a091af4c4969c4657a64d. Read-only hosted migration inventories before/after identical; both versions applied. | B2 delivery, final committed guard run and PR checks. No migration deployment needed or authorized. |
| E1 / MYK9-406 | Dedicated plan metadata checker and CI gate; 34 status/index records reconciled and 12 explicitly completed plans archived with link repair. Eight metadata tests and existing 21 doc-staleness tests passed; checker green. Missing marker, bad index, and misplaced section-status fixtures fail independently. | Final diff/link review, PR/CI; uncertain historical closure remains explicitly owned rather than invented. |
| F1 / MYK9-408 | Removed stale Codex-state assumptions; prepared exact failover prompt with coverage/report/range validation and no empty-window duplicate stamp. Candidate preserves frontmatter and matches repository prompt after documented wrapper removal. Codex automation ACTIVE verified; prior owner record says Claude disabled. | Fresh Claude owning-scheduler state, separately authorized installation and post-install parity, subsequent-day single genuine report/stamp. No scheduler mutation performed. |

## Verification and limits

Final focused app/edge run: 14 files, 228 tests passed, including notifier and health contracts. Final root guard/harness/metadata run: 4 files, 28 tests passed. Full lint passed (0 errors, 18 existing warnings). App and test TypeScript checks passed. Monorepo typecheck completed 25 tasks before the E2E tsx CLI hit sandbox IPC EPERM; rerunning its actual script using `node --import tsx` passed the ratchet: 59 current diagnostics, 62 baselined, zero new. Code-quality ratchet and E2E suite-map check passed. OpenSpec validation and `git diff --check` passed.

Browser replay used real site-admin authentication at local port 5409, fixture health-snapshot/operator-alert reads, and existing routes `/admin/health` and `/admin/dashboard`. Both 1440×900 and 768×1024 replays passed (2 Playwright tests, 10.9 seconds). All three ACL links opened the exact approved GitHub runbook via keyboard; payout links retained the JavaScript sentinel and internal navigation. Screenshots are temporary evidence under `/tmp/myk9-df66-{health,dashboard}-{1440,768}.png`. The unique interactive browser session was explicitly closed; the automated wrapper cleaned its own session. Console errors were localhost CORS failures from the separate Sentry metrics function; no all-console-clean claim is made.

Temporary logs and mutation evidence use `/tmp/myk9-df66-*`. Durable contract assertions remain in the committed-candidate tests and SQL fixture. [Source/bundle and prompt parity](linear-todo-source-parity-2026-09-05.md) records hashes and deployment/install boundaries. [Metadata reconciliation](plan-metadata-reconciliation-2026-09-05.md) records each plan disposition. No permission, role, ACL, scheduler, or shared database mutation was made.

## Delivery sequence

Final review additions: the edge runner's 60 tests pass, including an old 26-hour public-schema snapshot carried forward as 48 hours without changing its timestamp or verdict. Removing the cadence key makes that exact-value test fail (93600000 versus 172800000); restoring it passes. Changed-document link audit found only three pre-existing broken historical destinations (data-access ADR, old secretary OpenSpec change, MCP-plan link); archive links and new evidence links resolve. The local Vite server was stopped after browser verification.

Keep seven repository slices separate as the plan specifies. Obtain applicable PR/shared-write approval only after reviewable local changes are prepared. Keep A1, B1, B2, C1a and F1 open through their explicit evidence gates; a source merge alone does not close them. Use the required other-harness review before merge. Do not retry the hung full suite in a loop, manufacture hosted mutation proof, or create a reminder for the subsequent-day gate.
