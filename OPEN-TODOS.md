# Open Todos

Active work items only. Resolved historical context lives in git history and dated plan docs.

> **Launch sequencing lives in [`docs/plan-launch-execution-lanes.md`](docs/plan-launch-execution-lanes.md)** (canonical, lane-ordered as of 2026-06-16). Spin each _lane step_ into its own conversation. This file remains the detailed item inventory.

---

## Improve-Audit 2026-07-11 — nine plans + backlog triage

The plan docs live on `main` under [`docs/improve-audit-2026-07-11/`](docs/improve-audit-2026-07-11/) (index + plans 001–009, authored by the 2026-07-11 Fable pass). This section is the todo-list tracking layer for executing them — the docs shipped straight to `main` without OPEN-TODOS entries. Each plan carries its own verification gates and done criteria. Suggested order 001 → 002 → 004 → 005 → 003 → 006, though none block each other. Full context per item in TO-DOS.md § "Improve-Audit 2026-07-11 — ...".

- [x] ~~**002 — Date-util + enum-map hardening**~~ — **DONE 2026-07-11** (branch `claude/harden-date-util-enum-map`, TDD). `toLocalDateOnly` now preserves the calendar date of a UTC-midnight timestamptz shape (`/T00:00:00…(Z|+00:00)$/` → string slice) so a DATE column no longer round-trips a day early west of UTC; picker semantics unchanged. `PaymentPendingIndicator` (`statusConfig[status]`) and `HealthTimeline` (both the render path and the CSV-export path) fall back to a default config instead of throwing on an unknown enum value. Step-4 completeness grep clean. Evidence: 3 new/extended test files (12 assertions, each red-first), `pnpm typecheck` 26/26, lint clean, full app suite 11,980 passed. Plan: [`002-date-util-and-enum-map-hardening.md`](docs/improve-audit-2026-07-11/002-date-util-and-enum-map-hardening.md).
- [~] **003 — Typecheck the tests** — **Stage 1 implemented 2026-07-12:** root `pnpm typecheck` now blocks on a green 14-file allowlist; the separate non-E2E inventory reports 1,034 errors across 294 files after ambient declarations and Playwright separation. **Remaining:** admit cohesive test areas incrementally, correct test-side mock/fixture drift, and resolve reported runtime-contract mismatches before admitting affected files. Plan: [`003-typecheck-test-files.md`](docs/improve-audit-2026-07-11/003-typecheck-test-files.md).
- [x] ~~**004 — `ws` override bump**~~ — **DONE + MERGED 2026-07-11** ([#1273](https://github.com/rbeezley/myk9-platform/pull/1273)). `ws` 8.20.1→8.21.0 (exact patched version for GHSA-96hv-2xvq-fx4p), advisory cleared, full suite green (11,952 tests). Review-verified clean.
- [x] ~~**005 — Publish judge/steward guide**~~ — **DONE + MERGED 2026-07-11** ([#1274](https://github.com/rbeezley/myk9-platform/pull/1274)). Wired into the docs-site generated-content pipeline + header nav/landing card/sidebar (Astro build green). Publishes at `qa-draft`/noindex — customer-facing `verified` flip stays a separate gate (items 27–28).
- [x] ~~**006 — INTENT.md §6 + debt registers**~~ — **DONE + MERGED 2026-07-11** ([#1275](https://github.com/rbeezley/myk9-platform/pull/1275)). §6 rewritten to the unified `/at-show` reality; debt registers corrected to the measured over-500-line count of **175** (was stale "0 open"). Two archived plans `git mv`'d + index rows pruned.
- [ ] **007 — Split `MutationManager.ts` / extract `ReplicatedTable` query ops** — revised risk assessment (decomposition ~60% done, characterization tests mostly already exist, only ~10 pinning tests needed); frozen contracts, invariants, and phase gates fully specified. Plan: [`007-replication-core-split.md`](docs/improve-audit-2026-07-11/007-replication-core-split.md).
- [x] ~~**Class-status PO decision aid (#3)**~~ — **DONE 2026-07-12.** PO confirm pass complete: owner accepted all six recommended edge-case answers as-is (no overrides), confirmed the required override-marker column (`status_source`/`status_override`), and locked implementation candidate **(A) Supabase trigger** on `entries` writes (extending precedent `20260525170000`). Decisions recorded in the archived [`docs/plan-class-status-auto-derivation.md`](docs/archive/plan-class-status-auto-derivation.md) decision log; stub status flipped Stub → **Ready to draft**. Follow-up below.
- [x] ~~**Implement class-status auto-derivation**~~ — **DONE + MERGED 2026-07-12** ([#1294](https://github.com/rbeezley/myk9-platform/pull/1294)). Server-authoritative completeness now excludes scratched/withdrawn/cancelled/pulled entries and accounts for absent/excused outcomes; offline-first manual Started/Complete overrides persist; late entries reopen completed classes and raise durable show-map attention; core and ringside client classifiers agree with the server. Migrations `20260712180000`/`190000` are live and rolled-back staging behavioral proofs passed. Decision record archived at [`docs/archive/plan-class-status-auto-derivation.md`](docs/archive/plan-class-status-auto-derivation.md); OpenSpec change archived after spec sync.
- [ ] **Preserve explicit class-status intent across token-advance resend** — Follow-up from #1294 review: `rebuildUpdatePayload` currently strips all server-owned class-status keys on resend, so a queued-but-unsent manual Mark Complete/Started caught by token-advance reconciliation must be re-issued. Persist the original mutation's explicit key set alongside `PendingMutation` so resend reasserts exactly the intended `status`/`status_source` fields while unrelated class edits remain server-wins. Narrow and self-correcting; not launch-blocking.

### Backlog conversions (from README triage, 2026-07-12)

The [audit README](docs/improve-audit-2026-07-11/README.md) "Backlog (found, vetted, deliberately not planned)" items, promoted to tracked todos so they aren't lost. File paths + counts verified 2026-07-12. Direction-option decision recorded: **prioritize (C)** — verify `/at-show` offline + tablet maturity before first show day (executed via the prioritized "Manual browser round-trip walk of judge scoring at `/at-show`" item below); (A) onboarding seam and (B) day-of plan cluster are already covered (docs-guide todos / Plan 008 respectively).

- [ ] **Shared read-builder for the `reads.ts` trio** — `services/database/entries/reads.ts` (862 L) + `judges/reads.ts` (771 L) + `dogs/reads.ts` (763 L) = **2396 L** of repeated query shape; extract a shared read-builder. Investigate-first: confirm the shapes genuinely converge before abstracting. M–L, architecture.
- [ ] **Consolidate the refund dialog pair** — `components/entries/management/RefundEntryDialog.tsx` vs `EnrollmentRefundDialog.tsx` may be parallel surfaces for one workflow. **Investigate-first** — confirm they aren't distinct lifecycle stages before consolidating. M, money path.
- [x] ~~**Repoint stale backtick path mentions of the 4 archived plans**~~ — **DONE 2026-07-12.** Follow-up to the 2026-07-12 `docs/README.md` status-column sweep ([`main` `82c192de3`](https://github.com/rbeezley/myk9-platform/commit/82c192de3)), which `git mv`'d four Complete plans into `docs/archive/`. Repointed **5 files** (not the 2 originally scoped — a grep found the inventory was incomplete): the 2 named prose backticks ([`docs/audits/2026-06-proactive-qa/db-advisors.md:3`](docs/audits/2026-06-proactive-qa/db-advisors.md), [`docs/improve-audit-2026-07/005-replication-occ-watermark-spike.md:118,131`](docs/improve-audit-2026-07/005-replication-occ-watermark-spike.md)) **plus 3 genuinely-broken clickable links** the todo missed (its "not clickable links, so nothing 404s" premise was wrong): [`docs/launch-milestone-qa-checklist.md:5`](docs/launch-milestone-qa-checklist.md), [`docs/goals/fall-2026-launch-readiness-scorecard.md:76,77`](docs/goals/fall-2026-launch-readiness-scorecard.md), and [`docs/improve-audit-2026-07/README.md:60`](docs/improve-audit-2026-07/README.md) — all now point at `docs/archive/…`. Verified no old-path clickable links remain and both archive targets resolve. Left untouched as intended: the [`docs/superpowers/plans/2026-06-19-entry-management-layout.md:1000,1007`](docs/superpowers/plans/2026-06-19-entry-management-layout.md) historical `git add` execution log, and the bare-filename narrative cells in `plan-active-docs-triage`/`plan-ux-journey-audit` (cosmetic prose, never 404).

---

## Day-of Plan Consolidation — residuals — 2026-07-12

Residual one-liners left after archiving the shipped day-of/ringside plans (context: [`docs/improve-audit-2026-07-11/008-secretary-dayof-plan-consolidation.md`](docs/improve-audit-2026-07-11/008-secretary-dayof-plan-consolidation.md); plans now under `docs/archive/`).

- [ ] **Manual browser round-trip walk of judge scoring at `/at-show`** _(PRIORITIZED 2026-07-12 — carries audit direction-option (C): verify `/at-show` offline + tablet maturity matches the absorbed myK9Q before first show day)_ — the final QA gate of the archived [`plan-atshow-ringside-writes.md`](docs/archive/plan-atshow-ringside-writes.md). Code + `atShowJudgeScoring.spec.ts` cover the wiring; this is a low-risk literal cold-session walk (write → sync → secretary results view), not plan work. Extend the walk to explicitly exercise the offline + tablet dimensions (C), not just the online round-trip.
- [ ] **Ringside passcode LOWs (from archived [`plan-ringside-entries-read-authz.md`](docs/archive/plan-ringside-entries-read-authz.md))** — (a) anon-user hard-delete / TTL sweep beyond the existing `20260625000100` cleanup; (b) CAPTCHA on `validate-passcode`. Rate-limiting is already covered (migration `20260710140000` + SA-024 in-flight, [#1286](https://github.com/rbeezley/myk9-platform/pull/1286)); these two are the remaining optional LOWs.
- [ ] **Cut redundant Realtime WAL load (ringside OCC remediation #3)** — owner decision + SQL to trim the publication set; verify against and fold into the realtime-publication audit (#584) rather than duplicating. Context: archived [`plan-ringside-occ-conflict-storm.md`](docs/archive/plan-ringside-occ-conflict-storm.md).

---

## Launch Priority Focus — 2026-07-03

High-judgment work to prioritize while strong-model access is available. These are umbrella tracking items; execute through the linked plan/audit docs and keep one small PR per concrete slice.

> The go-live runbook is authored (2026-07-04): [`docs/operations/go-live-runbook.md`](docs/operations/go-live-runbook.md) — the single ordered, gated launch-day document. Execute launch operator work through it.

- [~] **Finish exhibitor elderly UX remediation after PR #1188** — PR [#1188](https://github.com/rbeezley/myk9-platform/pull/1188) merged 2026-07-07 and closed the closed-entry gates, post-close entry recovery, dog measurement trust fixes, and onboarding confidence slice. Payment Confidence is now complete in the follow-up branch: My Shows/My Payments share one current-balance helper, My Payments shows Amount due, and payment history separates gross/refunds/net. **Remaining:** Exhibitor Show-Day Trust, plain exhibitor check-in language, Dog Profile Clarity follow-ups, and final elderly low-tech browser re-walk/archive. Handoff: [`docs/handoff-exhibitor-elderly-ux-outstanding.md`](docs/handoff-exhibitor-elderly-ux-outstanding.md). Plan/OpenSpec: [`docs/plan-exhibitor-elderly-ux-remediation.md`](docs/plan-exhibitor-elderly-ux-remediation.md), `openspec/changes/exhibitor-elderly-ux-remediation`.
- [ ] **Exhibitor UX remediation (2026-07-08 role-journey audit)** — tracked in openspec change `exhibitor-ux-remediation`; audit report [`docs/ux-audits/exhibitor-elderly-novice-2026-07-08.md`](docs/ux-audits/exhibitor-elderly-novice-2026-07-08.md). Overlaps the _remaining_ scope of the elderly-UX item above (show-day trust, plain check-in language): the new change's §5 supersedes those bullets — implement them here, don't do them twice.
- [x] ~~**Root-cause silent health-probe failure (LIVE DEFECT)**~~ — **RESOLVED / DEPLOYED 2026-07-12.** Live read-only evidence showed the scheduled path recovered: `daily-health-check` ran at 07:00 UTC and persisted a fresh `cron-health-check` snapshot at 07:00:02 (`overall_status=ok`). The earlier stale-Vault-key hypothesis was disproved: `vault.secrets.service_role_key.updated_at` remains 2026-06-28, so no credential repair preceded recovery. The July 11 HTTP failure itself is no longer reconstructable because the cron discarded the `pg_net` request ID and the corresponding `net._http_response` row expired. The actual root cause is therefore indeterminate; the proven defect was the observability gap. Repository remediation merged in [#1284](https://github.com/rbeezley/myk9-platform/pull/1284) and is now database/function live: migration `20260711200000` applied, `daily-health-snapshot-watchdog` active at 08:00 UTC as `postgres`, `cron-health-check` v5 deployed, and a manual Vault-backed dispatch returned HTTP 200 and committed a fresh snapshot. The snapshot's sole warning is expected bootstrap state until the watchdog's first scheduled run. Optional Sentry monitoring remains operator-blocked because `SENTRY_DSN`/`SENTRY_ENVIRONMENT` are not configured; track that only in `go-live-2026-07-11-gate-remediation` task 2.5 and the go-live runbook.
- [ ] **Confirm AKC electronic results recipient address before launch (LAUNCH BLOCKER)** — `supabase/functions/send-results/index.ts` hardcodes `results@akc.org` with a `⚠ confirm actual address before launch` comment; [`docs/roles/secretary-registry-closeout-verification.md`](docs/roles/secretary-registry-closeout-verification.md) flags `eresults@akc.org` as the possible correct address. Verify with AKC, update the map, and redeploy `send-results` (`--project-ref sojmvhhwsjxmfistvzbe`). Found by the secretary verification sweep (rows S7.1/S7.5).
- [ ] **Align registration entry-window guards on the show timezone (P2, Codex)** — the client entry-open/close guards ([`apps/myk9show/src/pages/RegistrationWizardPage/entryCloseGuard.ts`](apps/myk9show/src/pages/RegistrationWizardPage/entryCloseGuard.ts)) compare against the **browser-local** calendar day, while the authoritative server RPC `submit_show_entries` (migrations `20260708130000` close + `20260711190000` open) uses the **show's primary-trial timezone** (`trials.timezone`). For an exhibitor east of a show's tz during the opening/closing-day offset window (Codex example: 00:30 ET for an LA show opening "today"), the wizard can allow a submission the RPC then rejects with `42501` — _after_ the registration row is created. Fix: thread the show trial timezone (`getTrialTimezone` from `@/features/registries`) into **both** client blockers so client + server compute the same calendar day; pass a show-tz `today` at the call sites (`useRegistrationWizardState.ts`, `submitPaymentStep.ts`). Pre-existing for the close side; surfaced while adding the open guard (see the `canRegisterForShow`/entry-open branch). Add a TZ-boundary test mirroring `permissionValidation.canRegister.test.ts`.
- [~] **Secretary show details UX remediation** — PR [#1214](https://github.com/rbeezley/myk9-platform/pull/1214) merged + OpenSpec change `secretary-show-details-ux-remediation` archived (`openspec/changes/archive/2026-07-09-secretary-show-details-ux-remediation`). **Follow-up 2026-07-10:** a partial Setup re-walk found + fixed one more trust break the change missed — Setup "Publish readiness" always said `Premium PDF is not published yet` because `PublishReadinessBlock` read premium columns off the offline-replicated `show` (which never syncs them); now reads a shared `usePublishInfo` fetch so it matches the Premium List card (regression tests added). **Remaining:** the full mobile/tablet/desktop Setup + Show Desk manual re-walk — attempted 2026-07-10 but blocked by a staging Supabase `connection pool` outage (unrelated to code). See the "2026-07-10 Remediation Re-Walk" note in the audit doc.
- [x] ~~**Full-scope security audit + remediation before launch**~~ — **DONE 2026-07-04 — all 17 findings remediated + deployed.** **Audit DONE 2026-07-03** (pinned `28a72d23f`): [`docs/security-audit-2026-07-03.md`](docs/security-audit-2026-07-03.md) — 0 CRITICAL, 0 HIGH, 8 MEDIUM, 9 LOW, **no P0/P1**. Stripe money path + RBAC role-table mutations + ringside claim tier verified clean. **Mechanical batch DONE** — [#1099](https://github.com/rbeezley/myk9-platform/pull/1099), merged 2026-07-03 (`a5356a74a`): SA-001 scoring-fn REVOKE (the only cross-tenant tampering vector) + SA-003/009/010/012/014/015/016/017. **All 5 design plans DONE** (see "Security Remediation — OpenSpec Tracking" section): SA-002/007 scoping-RLS ([#1109](https://github.com/rbeezley/myk9-platform/pull/1109)), SA-006 role-map ([#1118](https://github.com/rbeezley/myk9-platform/pull/1118)), SA-008 people over-fetch ([#1131](https://github.com/rbeezley/myk9-platform/pull/1131)), SA-011 passcode throttle ([#1130](https://github.com/rbeezley/myk9-platform/pull/1130)), SA-004/005/013 email-fn authz ([#1111](https://github.com/rbeezley/myk9-platform/pull/1111) + deploys 2026-07-04). 8 MED + 9 LOW = 17 findings, all closed.
- [x] ~~**Offline show-desk late entry for brand-new dogs**~~ — **DONE 2026-07-08.** Pulled forward from Post-Fall on 2026-07-07 because the launch-critical use case is a secretary at a no-connectivity show taking a day-of entry for a dog not yet in myK9. PR [#1199](https://github.com/rbeezley/myk9-platform/pull/1199) merged 2026-07-07: existing late-entry wizard is local-first for staff non-card submissions — local exhibitor queue, dependency-aware dog create, pending registration intents, and replicated day-of entries. OpenSpec change `offline-show-desk-late-entry` synced to `openspec/specs/offline-show-desk-late-entry/spec.md` and archived.
- [x] ~~**Fix account-level entry sync false-empty states**~~ — **DONE 2026-07-07.** OpenSpec change `fix-account-entry-sync-scope` closed after PR [#1159](https://github.com/rbeezley/myk9-platform/pull/1159) merged: empty local account-level entry reads verify the authenticated entry-results view while preserving offline fallback; sync-scope terminology cleanup landed with focused tests/typecheck/lint evidence. Main spec synced to `openspec/specs/account-entry-sync/spec.md`; OpenSpec change archived 2026-07-07.
- [~] **Execute/review the July bug-audit criticals** — Work through [`docs/improve-audit-2026-07/README.md`](docs/improve-audit-2026-07/README.md). **002, 004, 005 DONE** (merged/closed, see the plan README status table). **Remaining 2 broken out into individual items below** (see "July Bug-Audit — Remaining Items" section) so each is executed and checked off on its own.
- [x] ~~**Execute site-admin support/health remediation in small PRs**~~ — **DONE 2026-07-07.** Core phases 1-4 implemented in `site-admin-support-health-remediation`: dashboard health summary, support diagnostics links, health remediation metadata/history, and permissions data-trust/debug-label repair. First polish slice implemented in `codex/site-admin-support-health-polish`: template advanced-maintenance grouping and pure site-admin sidebar focus. Final tail closed in `codex/site-admin-support-health-tail`: repeated console-warning noise reduced for PerformanceObserver CLS observation and entry sync without show scope; admin help/search guard now proves deleted Alerts/Performance pages stay out of the live directory. Follow-up context lives in [`docs/plan-site-admin-support-health-remediation.md`](docs/plan-site-admin-support-health-remediation.md).
- [~] **Close out the money path and live-mode readiness** — Withdrawal/refund Phases 1–4 **SHIPPED + DEPLOYED 2026-06-27** ([#973](https://github.com/rbeezley/myk9-platform/pull/973)/[#974](https://github.com/rbeezley/myk9-platform/pull/974)/[#975](https://github.com/rbeezley/myk9-platform/pull/975); migrations pushed, Stripe fns redeployed; plan archived [`docs/archive/plan-refund-policy-withdrawal.md`](docs/archive/plan-refund-policy-withdrawal.md)). **Red-team pass DONE 2026-07-03** — pay→refund→payout, 3 parallel agents, findings in [`docs/security-audit-2026-07-03-money-path.md`](docs/security-audit-2026-07-03-money-path.md): **0 CRITICAL, 4 HIGH, 4 MEDIUM, 6 LOW**. **Close-out deployed 2026-07-06:** MP-01/02/03/04 are merged/deployed; snapshot-governed single-entry withdrawal refunds now execute server-side from `entries.withdrawal_policy_snapshot`; `stripe-webhook` waits for handler success before returning 2xx; entry checkout is card-only + ignores unpaid fresh sessions; refund and payout critical sections serialize through `show_money_locks` (`20260706184621`); go-live Stripe ID purge docs are mode-scoped. `20260625200000` and `20260626000000` were already remote; post-deploy dry-run reports the remote DB is up to date. **MP-06/MP-10 DONE + DEPLOYED 2026-07-08** ([#1218](https://github.com/rbeezley/myk9-platform/pull/1218), squash `58dc377e8`): `stripe-webhook`'s `charge.refunded` allowlist recognizes bulk show-cancellation refunds and alerts only when the intent's entries are actually unstamped, instead of flooding admin once per entry; `stripe-webhook` redeployed. **Code tail DONE + DEPLOYED 2026-07-09** ([#1240](https://github.com/rbeezley/myk9-platform/pull/1240), squash `974e2d598`): MP-05/07 fresh-session gate, MP-08 `operator_alerts` table + `/admin/health` surface (migration `20260709130000` pushed, live-verified), MP-09 fail-closed stamp guard, MP-11 refund detail UI, MP-12 deduped unmatched-refund alerts, MP-13 single confirmation email, TZ-01 timezone validation; 4 Stripe fns redeployed. Plan archived → [`docs/archive/plan-money-path-hardening.md`](docs/archive/plan-money-path-hardening.md). **Remaining:** only the operator-gated Stripe go-live tasks in Payments & Email below.

---

## Go-Live Gate Remediation — 2026-07-11

- [~] **Close the July 11 go-live and security-audit findings** — Tracked in OpenSpec change
  `go-live-2026-07-11-gate-remediation`. All repository/security slices are merged and deployed:
  lineage #1280, FORCE-RLS #1283, health #1284, mechanical fixes #1285, passcode #1286, premium
  throttle #1287, dedicated push secret #1289, and advisor sweep #1292/#1293. Live evidence covers
  passcode healthy/429/503 with alert recovery; premium catalog/concurrency/429/503 without Claude;
  and five Vault-authenticated inert push smokes plus service-role rejection. Custom SMTP was
  configured 2026-07-12 with a 100/hour Auth limit and a branded password-reset delivery to Gmail.
  Remaining work is operator-owned (legal, production data, Stripe live mode, mailbox/AKC,
  browser/device, Sentry routing, DNS/Vercel) plus a paid premium success smoke if approved. Migration parity is
  clean after #1294 merged remote versions `20260712180000`/`190000`; the post-merge
  `supabase db push --dry-run` reports the remote database is up to date.

---

## Security Remediation — OpenSpec Tracking — 2026-07-03

The 5 design-level plans from the 2026-07-03 security audit that weren't covered by the mechanical batch ([#1099](https://github.com/rbeezley/myk9-platform/pull/1099)). Each now has an apply-ready OpenSpec change (`openspec validate` clean) seeded from its source plan doc. Execute with `/opsx:apply` or the `openspec-apply-change` skill; check `pnpm exec openspec status --change <name>` for live per-task progress. Full context in TO-DOS.md § "Security Remediation — OpenSpec Changes".

- [ ] **Revoke auth identity on self-service account delete** — Follow-up to [#1257](https://github.com/rbeezley/myk9-platform/pull/1257) (Codex P1 remainder): `soft_delete_person` tombstones `people` and deactivates `user_roles`, but the `auth.users` identity can still sign back in (lands with no profile/roles). Needs a service-role edge function called after a successful self-delete. Full context in TO-DOS.md § "Self-Delete Auth-Identity Revocation".
- [x] ~~**Execute SA-002/SA-007 scoping-RLS remediation**~~ — **DONE + DEPLOYED 2026-07-03** ([#1109](https://github.com/rbeezley/myk9-platform/pull/1109), squash `2be73d70`). Show/trial-scoped `promo_codes` (INSERT/SELECT/UPDATE/DELETE, `created_by` pinned, validate-only RPC for exhibitors) + `trial_judge_supplies` (all four policies) via `can_manage_show`/`can_manage_trial`. Migrations pushed + verified live (all 8 policies scoped, none permissive). Codex review caught 3 findings (unscoped UPDATE/DELETE, club-vs-show wording, creator-integrity), all fixed. Change archived.
- [x] ~~**Execute SA-006 role-map disclosure remediation**~~ — **DONE + DEPLOYED 2026-07-04** ([#1118](https://github.com/rbeezley/myk9-platform/pull/1118), squash `daeea4d1b`). Mig `20260703180000` scopes the 5 `SELECT USING (true)` policies (`user_roles`→self-or-admin, `permission_audit_log`→admin-only, `roles`/`permissions`/`role_permissions`→authenticated deny-anon) + adds `get_show_officials`/`get_club_show_manager_ids` SECURITY DEFINER RPCs (REVOKE-from-PUBLIC then GRANT authenticated); `useShowOfficials`/`useEntryFormData`/`getClubShowManagerIds` repointed. Pushed + verified live (pg_policies/pg_proc). Codex 2nd opinion waived (rate-limited); migration-auditor + manual review clean. Change archived.
- [x] ~~**Execute SA-008 people over-fetch remediation**~~ — **DONE + MERGED 2026-07-04** ([#1131](https://github.com/rbeezley/myk9-platform/pull/1131), squash `6f3e20309`). Fetch gated to management roles (login + registration `HandlerSelectionDialog`) so exhibitor sessions never load the directory; `getAllUsers` column-allowlisted (no `select('*')`); `LazyDogCard` repointed to `dog.ownerName`; persisted store purged for non-management sessions (hydration- + auth-window-safe). 4 Codex rounds (3 with real findings, all fixed). Change archived (`openspec/changes/archive/2026-07-04-security-people-overfetch/`); specs promoted.
- [x] ~~**Execute SA-011 passcode throttle remediation**~~ — **DONE + DEPLOYED + VERIFIED LIVE 2026-07-04** ([#1130](https://github.com/rbeezley/myk9-platform/pull/1130), squash `aa506c81d`; change archived `openspec/changes/archive/2026-07-04-security-passcode-throttle/`). Recommended path: `upsert_ringside_session` now authorizes on the forge-proof `app_metadata` `ringside_passcode` claim instead of re-validating a raw passcode inline; heartbeat client stops sending the passcode; explicit `REVOKE ... FROM anon` (Codex caught that `revoke from public` left the prior explicit anon grant intact). Migration `20260704190000` pushed; **live checks**: anon EXECUTE=false, `validate_passcode` absent from body, `app_metadata` read; **rolled-back psql behavioral proof** confirms ALLOW (valid `ringside_passcode` claim upserts → show/role from claim) + DENY (raw passcode string ignored → non-entrant rejected). Full browser cold-session walk substituted by the psql proof (playwright-test MCP is a test-authoring harness, not ad-hoc live browsing; read/score path unchanged from the already-live-verified sibling claim tier `20260624163000`). Known soft behavior change: an account officiating via passcode no longer gets a presence row (read/score unaffected). Source plan: [`docs/security-audit-2026-07/plan-passcode-throttle.md`](docs/security-audit-2026-07/plan-passcode-throttle.md).
- [x] ~~**Execute SA-004/SA-005/SA-013 email-fn authz remediation**~~ — **DONE + DEPLOYED + VERIFIED LIVE 2026-07-04** (code merged [#1111](https://github.com/rbeezley/myk9-platform/pull/1111); change archived `openspec/changes/archive/2026-07-04-security-email-fn-authz/`, 3 specs promoted). Three email-origination edge functions now authorize their callers, deployed + live-tested: **SA-013** `send-waitlist-invite` v28 — shared-secret gate (`403` on missing/wrong secret); Vault `waitlist_invite_secret` + env `WAITLIST_INVITE_SECRET` provisioned to match (the `notify_waitlist_invite` trigger sends the header). **SA-004** `send-email` v58 — caller must be an authenticated show secretary/admin (`401` on unauthenticated call; was fully open) + per-user rate-limit. **SA-005** `send-auth-email` v45 — Standard-Webhooks HMAC verification, fail-closed on unset/invalid; env `SEND_EMAIL_HOOK_SECRET` matched the already-enabled dashboard Send Email Hook secret (no dashboard write needed), verified via a live password-reset (`send-auth-email 200` signature-verified + `resend-webhook 200`). Deploy coupling documented in [`docs/operations/supabase-auth-email.md`](docs/operations/supabase-auth-email.md). Source plan: [`docs/security-audit-2026-07/plan-email-fn-authz.md`](docs/security-audit-2026-07/plan-email-fn-authz.md).

---

## QA Program — execution order — 2026-06-12

Four steps toward the fall launch, in dependency order. Detailed phase checklists live in the sections below; this list is the master sequence.

## Exhibitor Entry Journey — 2026-07-10

Source: [`docs/ux-audits/exhibitor-entry-journey-elderly-ux-audit-2026-07-10.md`](docs/ux-audits/exhibitor-entry-journey-elderly-ux-audit-2026-07-10.md). Live staging walk at phone and desktop completed without payment or entry mutation after the test Heartland show’s entry-close date was extended through 2026-07-31.

- [ ] **Resolve contradictory exhibitor entry state** — Browse Shows says `Entry Submitted`, Show Detail says `My Entries 0`/empty, and Classes/registration show existing entered/cart state. Define and test one explicit display rule across the existing surfaces; do not add another entry dashboard. Tracker: `QA-STALE-DERIVED-STATE-035`.
- [ ] **Raise exhibitor entry/cart touch targets to 44px** — payment-summary remove (32px) and Cart “Continue Shopping” (40px) need the elderly touch floor, with focused component and 390px replay evidence. Tracker: `QA-ACCESSIBILITY-GAP-036`.

- [~] **4. Scorecard close-out (golden path sign-off)** — final launch gate: post-remediation re-walk of all **three** golden paths — secretary, exhibitor, and admin (the admin walkthrough has a lighter bar: support actions available or documented; it is not part of the UX journey audit) — flipping their `Unknown` rows in [`docs/goals/fall-2026-launch-readiness-scorecard.md`](docs/goals/fall-2026-launch-readiness-scorecard.md) against the scorecard's own pass thresholds. Then sweep the remaining scorecard dimensions, attaching evidence produced by steps 1–3 (offline-first behavior and data correctness from the dynamic-QA chaos/drift work, UX clarity from the journey audit, test/CI health from flaky quarantine) and listing what still lacks evidence. **Secretary + Exhibitor sweep DONE 2026-06-18 (Lane 1.6):** both golden-path rows flipped to **Green** (gated on real-user test) + all 10 primary dimensions swept ([close-out summary](docs/goals/fall-2026-launch-readiness-scorecard.md#scorecard-close-out--2026-06-18-lane-16)) — 3 Green / 6 Yellow / 1 Unknown / **0 Red, no open P0/P1**. **Admin walkthrough DONE 2026-06-25** (code+docs walk against the lighter bar; row flipped **Unknown → Green†** — all 5 capabilities available-or-documented; in-app ticket triage now lives at `/admin/support`, and the two SQL-only gaps (impersonation, arbitrary repair) remain accepted for fall + documented in [`docs/operations/admin-support-runbook.md`](docs/operations/admin-support-runbook.md); optional fast-follow = live spot-check once a SITE_ADMIN staging login exists). **July UX walk remediation DONE 2026-07-05** — [`docs/archive/plan-ux-walk-remediation-2026-07.md`](docs/archive/plan-ux-walk-remediation-2026-07.md) closed with focused unit, typecheck/lint, Chromium a11y, shell matrix, registration, and secretary UAT evidence. **Remaining:** close the launch gate via real-user testing (Lane 1.7) + the Yellow gaps.

---

## OCC Conflict-Storm — second incident — 2026-07-11

- [x] ~~**Ship `ringside-occ-conflict-circuit-breaker` (server-side OCC storm containment)**~~ — **DONE + DEPLOYED + ARCHIVED 2026-07-11** (PRs [#1271](https://github.com/rbeezley/myk9-platform/pull/1271) `3dc0dd319` + [#1272](https://github.com/rbeezley/myk9-platform/pull/1272) `ef1782ba` merged; change archived `openspec/changes/archive/2026-07-11-ringside-occ-conflict-circuit-breaker`; specs synced). Staging hit >80% CPU again 2026-07-11 (finding `QA-INFRA-OCC-STORM-037` in [`docs/qa/findings.md`](docs/qa/findings.md)): a stale-bundle Playwright client under the old persistent Codex nightly heartbeat looped `ringside_update_entry` conflicts at ~70/sec for 12+ hours (1,841 zombie sessions). Codex review converged over 5 rounds on scoping the client OCC cap to RPC/delta (the storm vector) and leaving full-row to its existing resolver. Live on staging: migration `20260711150000` applied (early cheap conflict rejection + rollback-proof `ringside_conflict_seq` counter; `authenticated` EXECUTE restored — scoring re-enabled behind the fix), `cron-health-check` redeployed (`ringside_conflicts` check live), behavioral proof green (counter 0→1→2 despite aborts, 40001+DETAIL at the precheck). Ops-side fixed same day: Codex nightly converted to standalone job (30-min hard kill, 1 worker/0 retries, ringside writes prohibited). Follow-up REVOKE migration `20260711160000` (Supabase default-privilege sequence-grant gotcha) merged. Separately flag: pre-existing duplicate-timestamp collision `20260710160000_self_service_soft_delete_person.sql` blocks normal `supabase db push`.

---

## OCC Conflict-Storm Follow-ups — 2026-06-25

Two follow-ups from the ringside OCC conflict-storm incident (PR [#961](https://github.com/rbeezley/myk9-platform/pull/961), which fixed the storm itself). A since-fixed unthrottled OCC-conflict bug let repeated/concurrent E2E runs against shared staging drive a sustained high-CPU write storm (Supabase >80% CPU alert, 2026-06-25).

- [x] ~~**Fix the stale-OCC-token root cause (replication sync-down)**~~ — **DONE 2026-06-25** (PR [#963](https://github.com/rbeezley/myk9-platform/pull/963), squash `882c1a8dd`). A non-conflicting dirty sync-down now reconciles the row (3-way merge of server-changed untouched fields + forward-only token advance + base advance) **and** the queued mutation (the upload reads the `PendingMutation` snapshot, not the IDB row — RPC deltas get the token advance; full-row UPDATEs also get a rebuilt payload via the new `rebuildUpdatePayload` adapter hook, else are left untouched to avoid a clobber). #961 remains the reactive net. Plan archived: [`docs/archive/plan-replication-stale-occ-token-sync.md`](docs/archive/plan-replication-stale-occ-token-sync.md).

---

## Operator Daily Health Board — 2026-07-04

Automate the recurring parts of the go-live runbook ([`docs/operations/go-live-runbook.md`](docs/operations/go-live-runbook.md)) Phase 5 into a morning health snapshot the site-admin reads on one page. Two changes sharing one contract table (`public.system_health_snapshots`); ship **sequentially** via `/opsx:ship` (page/migration first, runner second). Full brief + paste-ready invocations captured in the 2026-07-04 session.

- [x] ~~**Ship `admin-system-health-board`** (first)~~ — **DONE + MERGED 2026-07-04** ([#1123](https://github.com/rbeezley/myk9-platform/pull/1123), `c919a7e43`). `/admin/health` page + `system_health_snapshots` table (migration + RLS site-admin SELECT / service_role INSERT); renders per-check green/amber/red, flags stale (>26h) as its own failure. Change archived (`openspec/changes/archive/2026-07-04-admin-system-health-board/`).
- [x] ~~**Ship `admin-system-health-check-runner`** (after the board merges)~~ — **DONE + DEPLOY-VERIFIED LIVE 2026-07-04** ([#1125](https://github.com/rbeezley/myk9-platform/pull/1125), archived `d3b30402b`). Edge fn `cron-health-check` (secret-gated, service_role) → `system_health_probe` RPC → `buildSnapshot()` → INSERT; daily Vault cron `daily-health-check` @07:00 UTC. All migrations applied on remote, fn deployed (v1 ACTIVE, `verify_jwt=false`), 3 Vault secrets set. First real snapshots captured via a privileged-psql invoke of the cron's exact Vault-reading `net.http_post` (`source=cron-health-check`, `payout_cron`=ok / `migrations`=ok / `background_jobs`=warn only because `daily-health-check` has no scheduled run yet — self-referential bootstrap that self-heals to green at the first 07:00 UTC run). Change archived.

---

## UI Motion Consistency — 2026-07-03

- [x] ~~**Implement the motion-consistency plan**~~ — **DONE + MERGED 2026-07-05.** Phases 1–3 + 5 shipped in [#1143](https://github.com/rbeezley/myk9-platform/pull/1143) (squash `877647d33`): deleted the dead optimistic-UI motion cluster (live `useOptimisticScoring` engine preserved) + 2 `useReducedMotion` shims; added duration/easing motion tokens to `tailwind.config.js` + `DESIGN.md` spec; fixed the unstyled ringside `.success-toast` bug plus a `useAutoDismiss` timer hook; added status-chip color crossfade + non-virtualized `EntryCardGrid` reorder motion with a react-window guard. Phase 4 spinner→skeleton convergence shipped via [#1152](https://github.com/rbeezley/myk9-platform/pull/1152), [#1153](https://github.com/rbeezley/myk9-platform/pull/1153), [#1154](https://github.com/rbeezley/myk9-platform/pull/1154), and final sweep [#1157](https://github.com/rbeezley/myk9-platform/pull/1157); current `animate-spin` usage is limited to inline/action/progress contexts allowed by the spec. Plan marked complete in [`docs/plan-motion-consistency.md`](docs/plan-motion-consistency.md).

---

## Active-Docs Triage — net-new backlog — 2026-06-14

Source: [`docs/plan-active-docs-triage-2026-06-14.md`](docs/plan-active-docs-triage-2026-06-14.md). Items below were genuinely untracked elsewhere. Plan-backed features link to their plan instead of duplicating sub-tasks.

- [ ] **AI natural-language access / MCP / BYOK evaluation** (support/admin enhancement, post-critical launch work) — phased plan for reducing Ask Queue token cost, adding admin read diagnostics, exposing a remote read-only MCP server for Claude/ChatGPT power users, and evaluating BYOK without replacing existing myK9 workflows. **V1 narrowed 2026-06-29:** local, site-admin-only, read-only myK9-aware MCP diagnostics for Codex/Claude; no AskQ replacement and no write tools. Plan: [`docs/plan-ai-natural-language-access.md`](docs/plan-ai-natural-language-access.md). V1 plan: [`docs/plan-site-admin-mcp-v1.md`](docs/plan-site-admin-mcp-v1.md).
- [x] ~~**Multi-registry config layer — scent work, AKC + UKC + ASCA (DAY-ONE LAUNCH COMMITMENT, decided 2026-06-29)**~~ — **DONE 2026-07-01.** Phases 0–5b completed 2026-06-30 ([#1040](https://github.com/rbeezley/myk9-platform/pull/1040)–[#1056](https://github.com/rbeezley/myk9-platform/pull/1056)): registry config layer + all three registries populated from real rulebooks; class generator, entry blank, landing pages, confirmation emails, premium PDF ordering, and move-up eligibility all registry-aware; two live bugs found and fixed along the way (UKC premium sort order, UKC/ASCA move-up silently rejected). Phase 6 regression sweep clean (full `pnpm typecheck`/`pnpm lint`/app `vitest` — 10,399 tests). Phase 5c live cold-session staging walk complete: UKC + ASCA trial creation verified premium PDF, landing page, entry blank, confirmation email, and wizard rendering. ASCA premium-document generation remains a separate, larger tracked gap (DB CHECK constraint + edge-function gate predate the registries system). Plan: [`docs/plan-multi-registry-scent-work.md`](docs/plan-multi-registry-scent-work.md).
- [ ] **User documentation & support library** (pre-launch, large) — 23-task plan. **Planning complete 2026-06-19:** Phases 1–5 done (support ops docs, role guide outlines, training materials, blog content system, QA checklist). Remaining work is Phase 6 — drafting and verifying final customer-facing guides with screenshots. Readiness gates below. Plan: [`docs/plans/2026-06-12-user-documentation-support-plan.md`](docs/plans/2026-06-12-user-documentation-support-plan.md).
  - [x] ~~**[docs] Ringside quickstart — draft**~~ — **DONE 2026-06-24.** Drafted [`docs/user-guides/judge-steward-quickstart.md`](docs/user-guides/judge-steward-quickstart.md) from the outline (flag-removal PRs [#947](https://github.com/rbeezley/myk9-platform/pull/947)/[#948](https://github.com/rbeezley/myk9-platform/pull/948) merged → surface unblocked). Single-page printable quickstart, house style; reconciled to the #948 permanent "Ringside" sidebar entry (three access paths, not two). J-01…J-06 + access-paths diagram referenced inline as `pending capture` (no broken embeds); index + outline status updated. Remaining to `verified`: capture shots (item below) + author walkthrough.
  - [x] ~~**[docs] Capture J-01…J-06 + `at-show-access-paths` diagram**~~ — **DONE 2026-06-25.** J-01…J-06 (`screenshot-docs` skill + Playwright against staging, `e2e-judge@test.myk9.com`) and the `at-show-access-paths` draw.io diagram are both captured and embedded in [`docs/user-guides/judge-steward-quickstart.md`](docs/user-guides/judge-steward-quickstart.md); shot-list table fully updated. Fixed a stale "pending capture" banner left in the guide's intro/checklist text 2026-07-08.
  - [ ] **[docs] Promote guides `qa-draft` → `verified`** — Final accuracy pass on each guide (secretary, exhibitor, club-admin, judge-steward) once its blocked shots are captured. Each guide carries _"Do not publish to customers until status is `verified`."_ Depends on the non-author reviewer task below + all blocked shots cleared. Secretary + exhibitor are closest (only E-08/E-13 outstanding for exhibitor; secretary is fully captured).
  - [ ] **[docs] Recruit non-author reviewer** — Required before any guide reaches `verified`. Must be a person unfamiliar with the codebase (a trial secretary or club member). Name them in the QA checklist before the guide reaches `draft-ready`. See [`docs/user-guides/documentation-qa-checklist.md`](docs/user-guides/documentation-qa-checklist.md) Part 4.
  - [x] ~~**[docs] Staleness check**~~ — **DONE 2026-06-25.** Script [`scripts/check-doc-staleness.js`](scripts/check-doc-staleness.js) + `pnpm qa:doc-staleness:strict` alias wired into the `quality` CI job (`.github/workflows/ci.yml`). PRs that rename or remove a documented route now gate on the `Quality Checks` required check until the source map is updated. Optional follow-ups parked: extend to diff label _text_; screenshot >30d staleness.

Lower priority / post-launch (parked): judge-to-show matching + availability blackout-dates UI; early-access personal-club auto-create / cohort throttling / admin waitlist UI; re-run `/harden` for wizard medium/low findings.

---

## Code-Quality Audit Follow-ups — 2026-06-12

Source: PR [#642](https://github.com/rbeezley/myk9-platform/pull/642), PR [#647](https://github.com/rbeezley/myk9-platform/pull/647), `docs/audits/2026-06-code-quality/`.

**Needs-human tail** (from `docs/audits/2026-06-code-quality/` SUMMARY/08/09 — extracted 2026-06-14 so it isn't orphaned when that folder archives):

- [ ] **Remove completed kill-switch flags post-launch** (`showPresence`, `showLiveSync`, `showEditAwareness`, `showConflictSurfacing`) — Decided 2026-06-15: **keep through the first live shows** as no-redeploy launch-day safety for realtime show-day features, then remove the flags + inline the enabled paths. All four are `true` and live-validated; env overrides remain for E2E. Revisit after launch.
- [~] **Split Pull Management local pull state from online refund/accounting metadata** — **Phase 1 SHIPPED [#966](https://github.com/rbeezley/myk9-platform/pull/966) (2026-06-25):** type split + terminology rename — `ScratchRequest`→`PullRecord` (refund fields removed; `pull_timing`/`pull_reason`/`pulled_at` added), all scratch→pull lifecycle functions renamed (DB audit-action strings + `entry_status` enum values preserved), refund UI removed from `PullManagementTab` (refund work redirected to Entry Management). **Phase 2 deferred to post-Stripe:** reconciliation surface in Entry Management (filter=pulled, `pull_timing` derivation) + pre-payout advisory gate. Plan: [`docs/plan-pull-management-split.md`](docs/plan-pull-management-split.md).
- [~] **P3 config hygiene** — clear-cut cleanups done in `chore/p3-config-hygiene`: CDN name drift reconciled to `VITE_CDN_BASE_URL`; monitoring section of `.env.example` reconciled to actually-read vars; unread `VITE_ENABLE_DEV_TOOLS`/`VITE_ENABLE_DEBUG_LOGS` removed; broken `phase8:*` scripts + dead runner `.js` files removed; `test:load:full` retargeted to platform-neutral spawn; dead early-adopter dog-tool flags deleted; stale conflict-surfacing comment fixed. **Deferred (needs human/Vercel):** removing `VITE_APP_ENVIRONMENT` (documented in VERCEL-SETUP.md, may be set in Vercel); removing completed kill-switch flags (`showPresence`/`showLiveSync`/`showEditAwareness`/`showConflictSurfacing` — product call); adding missing operator vars to `.env.example` (separate P2 item).
- [x] **Consolidate the show-detail surface (hotspot #1)** — **DONE 2026-06-29** across 8 merged PRs ([#1032](https://github.com/rbeezley/myk9-platform/pull/1032)–[#1038](https://github.com/rbeezley/myk9-platform/pull/1038); plan archived to [`docs/archive/plan-show-detail-consolidation.md`](docs/archive/plan-show-detail-consolidation.md)). De-fused both churn-topping files: `ShowDetailsPage.tsx` 893→466 LOC (now a thin audience router → `ShowPublicLanding` / `ShowExhibitorView` / `ShowManagementShell` + shared `ShowDetailTabs`; data via `useShowLandingData`, decision via `resolveShowAudience`), and `TrialDetailsPage.tsx` 606→383 LOC (`useTrialDetailData` + `TrialManagementDialogs`). Every extracted module is under the 500-line ceiling; all phases behavior-preserving with new per-module unit tests. Dual-route kept (unscoped `/trials/:trialId` is an active fallback). **Not in scope:** `EntryManagementPage.tsx` (499, at the ceiling — a separate concern, not part of the show/trial audience fusion).

---

---

## Show-Day Secretary Workflow Brainstorm — 2026-05-16

Captured from a brainstorm covering show-day tasks the secretary performs. Confirmed during the session: single-show-at-a-time is the dominant case; secretaries may have other future or other-club shows visible but only run one at a time. `useMissionControlData` already exposes a single "selected show" concept and persists to localStorage + showStore, so a show-centric IA refactor does not require a data-layer rewrite.

### Design principle — guided, non-blocking UX

Cross-cutting principle that informs every other todo in this section. Secretaries — especially newer ones — often do not know what to do next. Other software fails them by being a passive tool palette. We should make the system _opinionated about the next action_ while never blocking the secretary from doing something else. **Not a wizard** (wizards are modal and linear; show day is parallel and interrupt-driven). Instead: the system always has an advisory recommendation, surfaced as a non-modal card, that the secretary can act on, dismiss, or ignore.

### Information architecture

**Anchor view: extend the existing show-map tree** (`apps/myk9show/src/features/show-map/ShowMapTab.tsx`) rather than designing a new workbench from scratch. The tree already provides: hierarchy (show → trial → class → entry), per-node status, per-class progress, and an attention count that aggregates up. The mode tabs (Setup / Today / Wrap-up) become _decorations on the same tree plus a flat queue on Today_ — they change the default filter, the row actions, and which auxiliary strips/views are shown. The tree is the orientation layer ("where is X?"), not the whole command center; for urgency ("what now?") the Today tab adds a flat priority queue rendering above the tree, both consuming the same priority function. Pages folded into row actions / sub-views: most of `DayOfOperationsPage` (Check-In / Move-Ups / Pulled / Day-of Entries), `VolunteerSchedulingPage` (into Setup tab), `RunOrderPage` personnel tab (into Setup tab). Pages kept as drill-in destinations: show edit, class edit, entry edit, `PaperScoresheetPage`, run-order detail editor.

**Deprecation mechanics (per page, explicit):** `/secretary/dashboard` → keep as multi-show overview only (the show picker when more than one active show); auto-route to the active show's workbench when there is exactly one. `/secretary/day-of` → 301-redirect to the workbench's Today tab. `/secretary/run-order` → 301-redirect to the workbench's Setup tab, anchored to the run-order section. `/secretary/volunteer-scheduling` → 301-redirect to the workbench's Setup tab, anchored to the personnel section. The `PipelineDashboard` component (already unrouted per the explore) → delete after its callers are migrated. **No bare delete-without-redirect on previously-routed pages** — redirects preserve email/Slack/bookmark links. Capture the redirect map in the consolidation PR description.

    **Smart menu refinement:** every menu opens with a **Recommended** section at the top (max 2 items, separated by a divider from the full action list), each with a leading icon and a brief _why_ line in muted text (e.g., "Class starts in 20 min", "Scores entered but not finalized"). The full action list still appears below the divider in a stable order — no layout shift if no recommendation applies; the Recommended section is simply omitted. The recommendation is NOT the default keyboard target (deliberate Enter required to execute, to protect against destructive misclicks like Scratch). Recommendations must be deterministic within a render frame — no flicker.

    **Shared priority function constraint:** the Recommended section, the "Next Best Action" card, and the tree's Attention filter all derive from a single pure function (e.g., `getRankedActions(node | 'root', state)` in `features/show-map/`). Three callers, one function — scope is the only argument that varies. Do not let any caller embed its own priority logic; that is how the three surfaces would silently diverge.

### Reports & submission

- [ ] **Print testing on venue hardware** — Real-world print test for `CheckInSheet`, `ScoresheetReport`, `ResultLabels`, `ArmbandLabelsReport` on representative venue setups (label printer + standard laser). Capture any margin/scaling/duplex issues.

---

## Show Creation Wizard — UX findings — 2026-07-01

From live walk-throughs during the multi-registry Phase 4 verification. Full detail + calibration caveats in [docs/audits/2026-07-01-show-creation-wizard-ux.md](docs/audits/2026-07-01-show-creation-wizard-ux.md).

- [x] ~~**"Next" fails silently on Step 1 (P1)**~~ — **VERIFIED COMPLETE 2026-07-04.** The audit finding was already resolved 2026-07-01: `Next` stays clickable while validation fails, failed clicks expand + scroll the validation banner into view, and `WizardNavigation` shows the inline "N items remaining" hint before clicking. Focused verification passed: `pnpm exec vitest run src/test/components/WizardNavigation.test.tsx src/pages/secretary/__tests__/ShowCreationWizardPage.validationFeedback.test.tsx` (21 tests).
- [x] ~~**Step-1 density: group, don't split (P2)**~~ — **DONE 2026-07-04** ([#1129](https://github.com/rbeezley/myk9-platform/pull/1129), `refactor(wizard): group show details step sections`). `ShowDetailsStep` Step 1 regrouped into `steps/sections/*` — Basics · Dates & Entry · Fees & Payments · Officials · a collapsed-by-default "More options" (`MoreOptionsSection.tsx`) — matching the audit's plan.

---

## North Star — Phase 3: Real-User Testing

- [ ] **Phase 3 — Real-User Testing** _(DEFERRED to the final pre-launch gate — decision 2026-06-18)_ — Canonical real-user testing task: recruit 2–3 non-technical test users (one secretary, one or two exhibitors), hand them written tasks, watch silently, and fix every hesitation. **Re-sequenced to run last, after Lanes 2–4 + launch-affecting Lane 5**, so users test a near-final product (was Lane 1.7; the internal UX Journey Audit remediation + scorecard evidence pass are already done). Sole closer for the launch criterion "real-user testing completed" + the scorecard UX-clarity dimension. See [`docs/plan-launch-execution-lanes.md`](docs/plan-launch-execution-lanes.md#final-pre-launch-gate-runs-last--after-lanes-24--launch-affecting-lane-5). Full plan: `docs/plans/strategy/2026-04-11-north-star-fall-2026.md`.

---

## Payments & Email

- [x] ~~**Brainstorm automated show email sequence campaign**~~ — **SHIPPED as OpenSpec implementation in PR [#1202](https://github.com/rbeezley/myk9-platform/pull/1202).** Secretary-reviewed lifecycle emails now live in the existing Message Center/Entry Management flow: automatic online entry receipts stay transactional; accepted/waitlisted decisions open editable Send now prompts; Not now saves ready jobs visible in Scheduled emails and on entry rows; 2-week/day-before/results batches require Send now; correction emails are offered when a sent decision changes. Remote Supabase migrations applied through `20260708120000_show_lifecycle_emails.sql`. OpenSpec change: `show-email-sequence`.
- [x] **Brainstorm show presence roster + conversation placement** — Resolved as the Show Desk `People at show` tool: an all-exhibitors roster inside the existing Show Desk Tools drawer with presence dots, armband/dog/class lookup, direct eligible check-in, and deep links to existing Secretary Messages and Entry Management. Design: `docs/superpowers/specs/2026-07-08-show-desk-people-roster-design.md`. Implementation change: `openspec/changes/show-desk-people-roster`.

### Stripe follow-ups (from 2026-06-10 sandbox walkthrough)

- [x] **cron.schedule migration for payouts** — **DONE + VERIFIED 2026-06-27.** The placeholder-literal approach (`20260618130000_payout_cron_schedule.sql`, [#834](https://github.com/rbeezley/myk9-platform/pull/834)) was _superseded_ by the Vault-backed rework `20260619130000_payout_cron_vault_secret.sql` — both applied on staging. The real bug was **not** the migration: 2 of the cron's 3 Vault secrets (`service_role_key`, `payout_cron_secret`) were never set, so `nightly-show-payouts` (jobid 6) had failed every night since ≥Jun 23 with `Missing Vault secret: service_role_key` (`cron.job_run_details`). Fix: rotated `PAYOUT_CRON_SECRET` edge-fn secret + set the two matching Vault secrets; verified end-to-end by firing the cron's exact Vault-reading `net.http_post` → **HTTP 200** from `cron-process-payouts`. **Lane 4.5 real payout ALSO VERIFIED 2026-06-27:** backdated the Heartland demo show, ran the cron → `show_payouts` row `completed`, `amount_cents=9000`, real Stripe transfer `tr_1Tn6rZAIej2Q9UtXrzfsggkS`; dates restored. The full payout pipeline (eligibility → amount calc → `stripe.transfers.create` → completed) is proven with sandbox money.
- [ ] **Go-live (live mode) tasks** — runbook Task 6.3 + the new step 4 (purge sandbox-scoped `cus_`/`acct_` ids from `stripe_customers`/`exhibitor_profiles`/`club_stripe_accounts` — mode-scoped ids caused the 2026-06-10 "No such customer" failure in reverse). Also: rename Stripe account display name "Myk9t" → "myK9Show", grant founding members, and **set the live platform payout schedule to Manual** (default daily auto-sweep drains available balance and starves club transfers — see runbook "Platform payout schedule"; found 2026-06-10 when every sandbox transfer failed `insufficient_balance`).
- [ ] **Review + execute `docs/plan-stripe-golive-enforcement.md`** — the plan doc now exists on `main` (authored by the 2026-07-11 Fable pass); it defines gates/checks that block Stripe go-live until the manual go-live tasks above are verifiably complete. Read it, decide whether to adopt, and implement the enforcement mechanism. Full context in TO-DOS.md § "Stripe Go-Live Enforcement".
- [ ] **Author `docs/plan-stripe-live-cutover-runbook.md`** — Plan the ordered operator runbook for the actual sandbox→live Stripe cutover event; **does not exist yet** (only the enforcement plan shipped). Clarify overlap with the enforcement plan before writing, so the two don't duplicate. Full context in TO-DOS.md § "Stripe Live Cutover Runbook".

---

## Pre-Launch Housekeeping

- [~] **CI-gated Vercel deploys** — **Workflow MERGED [#887](https://github.com/rbeezley/myk9-platform/pull/887)** (`Deploy Production` workflow_run-gated on CI success + `head_sha` checkout + `vercel deploy --prod`). **Rollout pending — not yet active** (job gated behind the `PRODUCTION_DEPLOY_ENABLED` repo var, so inert on merge). Operator steps to activate, per [`docs/operations/ci-vercel-deploys.md`](docs/operations/ci-vercel-deploys.md): (1) add `VERCEL_TOKEN`/`VERCEL_ORG_ID`/`VERCEL_PROJECT_ID` secrets, (2) set `PRODUCTION_DEPLOY_ENABLED=true` + validate with auto-deploy still ON, (3) set `git.deploymentEnabled.main:false` in `apps/myk9show/vercel.json` to disable Git auto-deploy (NOT an Ignored Build Step — would abort the workflow's own deploy). Follow-up: gate the `apps/docs` guides project too.
- [x] ~~**Test Clone Show feature**~~ — **DONE 2026-07-01.** Clone Show is consolidated into the Show Creation Wizard as the single secretary workflow. The old Calendar-page clone dialog was removed. Selecting a prior show now prefills the wizard, copies settings plus trial/class structure, clears show/entry dates and trial schedule/event-number fields that must be confirmed fresh, and keeps the secretary stepping through the normal review path. Verified with focused unit coverage, secretary wizard E2E coverage, duplicate-surface search, and `pnpm typecheck`.
- [ ] **Author `docs/plan-akc-ukc-pdf-acroform-reports.md`** — Plan filling official AKC/UKC PDF AcroForm templates via `pdf-lib` (the report-generation gap left after HTML reports shipped); **does not exist yet** on `main`. Full context in TO-DOS.md § "AKC/UKC PDF AcroForm Reports".
- [ ] **Pre-load AKC & UKC Judge Directory** — importer **tooling DONE** ([#833](https://github.com/rbeezley/myk9-platform/pull/833): `scripts/import-judges.ts` + `supabase/seed-data/akc-ukc-judges.csv` template; CSV → idempotent `people` + `judge_qualifications` migration). Remaining: obtain real AKC/UKC exports, populate the CSV (still header-only), run `npx tsx scripts/import-judges.ts > migration.sql`, and push.
- [x] ~~**Refund policy matrix (voluntary withdrawal / show-cancellation / judge-change)**~~ — **DONE (verified 2026-07-10; both "Remaining" bullets had already shipped).** Plan archived Complete: [docs/archive/plan-refund-policy-withdrawal.md](docs/archive/plan-refund-policy-withdrawal.md). Phases 1–3 SHIPPED 2026-06-27 ([#967](https://github.com/rbeezley/myk9-platform/pull/967)/[#969](https://github.com/rbeezley/myk9-platform/pull/969)/[#970](https://github.com/rbeezley/myk9-platform/pull/970)); Phase 4 voluntary-withdrawal execution DEPLOYED 2026-07-06 (`stripe-refund-entry` snapshot-governed server-side amounts). **Show-cancellation make-whole** shipped in [#974](https://github.com/rbeezley/myk9-platform/pull/974) (`stripe-refund-show` + atomic `stamp_show_refund_entries` RPC + `RefundAllEntriesCard`, 29 tests) and was hardened/redeployed via [#1240](https://github.com/rbeezley/myk9-platform/pull/1240). **Deno resolver dedupe** done: `withdrawalSnapshot.ts`/`withdrawalPolicyText.ts` no longer exist — consolidated into `_shared/withdrawalPolicy.ts` (its header documents the merge). The remaining Deno↔app-`src` mirror is intentional (Deno can't import app `src`), not debt. A stale sibling "Refund-policy tail" line describing the same finished work was removed 2026-07-10.
- [ ] **WATCH-ITEM: ~30 remaining wall-clock perf asserts** — Reclassified from the completed Dynamic QA Infrastructure section on 2026-06-16. `expect(duration).toBeLessThan(...)` survives in 7 files: the DB-query suites (`userQueries` ×7, `dogQueries` ×7, `showQueries` ×6, `simplified-database-queries` ×6, `comprehensive-database-test-suite` ×2), `comprehensive-store` ×1, `ReplicatedClassesTable` ×1. Same machine-dependent anti-pattern as #761/#762, but empirically stable across the 18-seed shuffle sweep and mostly asserting 200ms budgets on mocked DB calls. **Do not proactively rewrite all 30**; fix reactively if CI reds under shuffle by converting that spec to a non-blocking log using the #762 pattern.

---

## Post-Fall (parked — do not pick up before Phase 3 exit)

- [x] ~~**Prevent Duplicate Rows in Core Tables**~~ — **DONE 2026-07-07.** Dog registry-identity prevention landed in OpenSpec change `prevent-duplicate-dog-identities` (`fix(dogs): prevent duplicate registry identities`): exact live `dog_registrations` identities get normalized uniqueness, duplicate-aware dog create, and existing-surface warnings. People/clubs follow-up landed in PR [#1197](https://github.com/rbeezley/myk9-platform/pull/1197) (`fix(identity): prevent duplicate people and clubs`): club normalized-name uniqueness + duplicate-aware create, people email guardrail consistency, and shared existing-surface candidate matching. Main specs synced to `openspec/specs/dog-identity-deduplication/spec.md` and `openspec/specs/core-identity-deduplication/spec.md`; OpenSpec changes archived 2026-07-07. Broader merge tooling / historical cleanup remains intentionally unbuilt unless real launch data proves it is still needed.
- [ ] **Configurable Exhibitor Convenience Fee** — ~~Add site-admin default~~ The site-admin-default half moved into the PRE-LAUNCH "Admin payout ledger + platform fee setting" item (2026-06-10). What remains parked here: the **per-show/per-club override** — build when a real club negotiates a custom rate (safe by construction: the fee never enters payout math).
- [ ] **Role-Mode Icon Switcher for Sidebar Nav** — Replace labelled section groups with an icon-mode switcher; brainstorm before implementing.
- [ ] **Review awesome-design-md for Design Consistency** — Evaluate against current dual approach (shadcn/ui + semantic CSS).
- [ ] **Research Claude Code Managed Agents for AskQ** — Evaluate managed agents API for the AskQ feature.
- [x] ~~**Unify "Add Entries" with whose-dog branching (secretary IA review)**~~ — **DONE 2026-07-03.** `ux-entry-payment-golden-path` added a secretary Add entries decision point that asks whose dog is being entered and routes to the existing exhibitor self-service or secretary/on-behalf wizard; no new registration surface was created.
- [x] ~~**Scope the entry wizard dog picker by audience**~~ — **DONE 2026-07-03.** `/shows/:id/register` now stays exhibitor self-service even for staff entering their own dogs, while `/secretary/register/:id` remains the all-dog secretary/on-behalf workflow with advanced search and create-new tools.
