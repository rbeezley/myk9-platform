/**
 * Scoring UI Utilities
 *
 * Shared utilities for time formatting and scoring calculations.
 */

export {
  formatMilliseconds,
  formatSecondsToMMSS,
  formatSecondsToTime,
  convertTimeToSeconds,
  formatTimeForDisplay,
  formatTimeLimitSeconds,
  parseTimeToMs,
  formatTimeInputToMMSS,
} from './timeUtils';
