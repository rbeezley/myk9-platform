## 1. MYK9-389 cloned class reachability

- [x] 1.1 Write a failing focused test for a renamed/non-template cloned class remaining searchable and removable in the existing Classes step, including Review count and judge-assignment updates.
- [x] 1.2 Render retained fallback class definitions in the existing selection grid without losing other cloned customizations, and verify the focused test passes.

## 2. MYK9-390 Create Classes template selection

- [x] 2.1 Write a failing browser-path/component test proving template selection advances beyond Step 1 and catches the current `require is not defined` path.
- [x] 2.2 Replace the browser-incompatible store lookup with the existing typed template-store API while preserving the class-creation store contract; verify store/component tests pass.
- [x] 2.3 Make template cards pointer/Enter/Space operable with an assistive selected state, and verify a class can be selected through Set Values and Review in focused coverage.

## 3. MYK9-391 canonical secretary audit fixture

- [x] 3.1 Update the repository role-journey audit skill to resolve the secretary identity from `testUsers.ts` and private environment configuration, and verify contradictory fixture-domain guidance is absent.
- [x] 3.2 Add or update source-contract coverage for valid and missing/invalid fixture instructions, verifying the recovery gap is precise and committed output contains no secret values.
- [x] 3.3 After explicit shared-system approval, update the existing weekly secretary automation prompt to the same fixture convention and verify its saved configuration matches the skill.
- [x] 3.4 Run a fresh read-only two-step secretary sign-in preflight, recording actual roles and verifying no password/token is present in logs or evidence.

## 4. MYK9-392 club show card actions

- [x] 4.1 Write failing focused interaction tests for a show-specific trigger name, Enter/Space opening without navigation, Escape focus return, direct card navigation, and a 44px target.
- [x] 4.2 Repair Upcoming Shows event boundaries and accessible trigger semantics, review/fix the Past Shows sibling, and verify focused tests pass without adding a duplicate workflow.

## 5. MYK9-393 Trial Secretary report policy

- [x] 5.1 Write assertion-first tests for the actual schema-backed 2025/2026 date boundary, unsupported/missing dates, and a 136-total/2-excluded 2026 fixture yielding 134 × $4.50 = $603.00 in both builders.
- [x] 5.2 Implement one typed year-aware eligible-run/rate/total policy consumed by preview and official PDF values; verify MYK9-317 withdrawn/scratched/cancelled coverage stays green.
- [x] 5.3 Remove or align stale preview instructions with canonical form JSW001 (11/25), and verify preview and downloadable form agree on counts, rate, total, timing, and address source.

## 6. Integrated verification and tracking

- [x] 6.1 Run focused unit/interaction suites for all touched components, stores, report helpers, and instruction contracts; record pass/fail counts in the change and Linear issues.
- [x] 6.2 Run app-local typecheck, lint for touched files, production build, `git diff --check`, and strict OpenSpec validation; resolve actionable failures.
- [x] 6.3 Replay MYK9-389, MYK9-390, MYK9-392, and MYK9-393 at 1440×1000, 768×1024, and 390×844, including the Create Classes production preview and Trial Secretary PDF field/visible-output inspection; attach sanitized evidence.
- [x] 6.4 Review the combined diff against each Linear execution contract, repository intent, consolidation, accessibility, offline-read, and money-path requirements; fix critical/warning findings and re-run affected checks.
- [x] 6.5 Update each Linear issue with files changed, tests/checks, branch/PR, risks, remaining work, and acceptance-criteria evidence; leave any issue with an unmet gate open.

## 7. PR, CI, merge, and archive

- [x] 7.1 Commit accepted implementation, push the feature branch, open one PR with all five Linear issues and `Tracked in openspec change: resolve-september-secretary-todo-batch`, then verify required CI/review checks.
- [x] 7.2 Obtain explicit approval before merge, merge only after checks and evidence gates pass, then move satisfied Linear issues to Done with PR and merge-commit evidence.
- [x] 7.3 Archive the OpenSpec change only after the PR is merged and required evidence is recorded; sync main, prune refs, delete the branch, and remove the worktree last.

## Validation Profile

- Risk: high
- Validation: full
- Rationale: The batch spans secretary setup, accessibility, shared audit automation, and registry remittance calculations; although it has no data mutation, incorrect report values or incomplete browser evidence could produce launch-critical operational harm.
