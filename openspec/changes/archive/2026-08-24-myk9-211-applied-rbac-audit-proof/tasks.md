## 1. Approval and baselines

- [x] 1.1 Obtain Richard's explicit confirmation of the corrected named staging fixture: actor `testadmin@myk9t.com`, target `club@myk9t.com`, temporary `secretary` role, and Heartland club scope `dededede-0000-0000-0000-000000000001`; confirm that the permanent audit residue is accepted. — Richard confirmed Phase 3 corrected the local fixture configuration, reset and verified all seven fixture credentials, updated eleven GitHub E2E secrets, and removed all `test.myk9.com` auth identities. His earlier approval of the target, role, scope, and permanent audit residue remains in force.
- [x] 1.2 Record current `origin/main` and staging deployment identity, verify the target has no matching active or inactive role assignment, and capture the target-filtered audit baseline plus UTC evidence-start timestamp. — `origin/main` and the worktree were both `7f83dc19bfe4d54e4494e7e0b4a08827a935db42`. GitHub production deployment `6068017957` binds that SHA to successful status `17261468248`, created 2026-08-24T17:36:10Z, at `https://myk9-platform-myk9show-p7wtfun7v-richard-beezleys-projects.vercel.app`; the browser replay used its current production alias `https://myk9-platform-myk9show.vercel.app`. Refreshed read-only baseline at 2026-08-24T19:58:23.586689Z found zero matching assignments and zero prior target audit rows.
- [x] 1.3 Confirm the admin and target are seeded test identities, the actor remains an active site admin, the target has only its expected exhibitor grant, and no real-user data is in scope. — Read-only inventory confirmed configured Test Admin (`testadmin@myk9t.com`, person `e144a774-9f34-409a-bb0d-8fad03ce97ea`) has active `site_admin`; Test Club (`club@myk9t.com`, person `91a6d9fc-fe77-41b7-88d5-5ae576458a99`) is a profile-only seeded fixture with only active `exhibitor`; Heartland is the seeded club. No real-user row is selected, and staging contains zero `test.myk9.com` auth users.

## 2. Applied mutation proof

- [x] 2.1 Sign in as the canonical site-admin fixture and invoke the current `RoleManager.assignRole` path to grant Test Club the `secretary` role scoped to Heartland; record the returned assignment ID. — Authenticated as `testadmin@myk9t.com`; `assignRole` returned `00024170-bb22-47a8-bf3c-f9852d5bab84` at 2026-08-24T20:01:22Z.
- [x] 2.2 Verify exactly one new `role_assigned` audit row names Test Admin as actor, Test Club as target, the secretary role ID/name, exact Heartland `club_id`, null `show_id`, and a timestamp within the evidence window. — Event `ea61ab25-36bb-483e-a18c-c83cda658684` at 2026-08-24T20:01:23.271961Z contains the exact actor, target, role, club, and null show payload.
- [x] 2.3 Invoke `assignRole` with a valid-format nonexistent `people.id`, require failure before insert, and prove no assignment or successful audit event is written. Do not attempt a duplicate scoped grant: the deployed nullable uniqueness constraint does not safely reject it. — Invalid person `00000000-0000-0000-0000-000000000211` was rejected by `RoleManager` before insert; database audit count for that target in the evidence window is zero.
- [x] 2.4 Invoke the current scoped `RoleManager.revokeRole` path, require `true`, and verify exactly one `role_revoked` row with the same actor, target, role, scope, and evidence-window timestamp. — Revoke returned `true`; event `7016a266-22b5-4419-ac34-a64741f780df` at 2026-08-24T20:01:23.98978Z contains the exact actor, target, role, club, and null show payload.
- [x] 2.5 Repeat the same revoke, require `false`, and prove no additional successful audit row appears. — Repeated revoke returned `false`; the target has exactly the two expected evidence-window audit rows.
- [x] 2.6 Verify cleanup: the exact assignment is inactive, Test Club has no active Heartland secretary authority, and the two append-only audit events remain. If any earlier assertion fails, run and verify authenticated cleanup before investigating; emergency direct deactivation contains authority but leaves the issue open because it cannot produce the required revoke audit event. — Assignment `00024170-bb22-47a8-bf3c-f9852d5bab84` is inactive, active matching authority count is zero, and both append-only events remain.

## 3. Browser and regression evidence

- [x] 3.1 Against staging at the recorded deployed commit, open `/admin/permissions` as Test Admin and confirm Recent access changes displays both Role Assigned and Role Revoked for Test Club with readable relative timestamps. — At 1440x900, the staging overview displayed consecutive `Role Revoked` and `Role Assigned` entries for `Test Club (club@myk9t.com)` with readable relative times and zero console errors.
- [x] 3.2 Open the deployed `/admin/permissions?tab=audit`, correlate the two browser rows with the database event IDs, and capture the initial private screenshot/checksum. — At 2026-08-24T20:03:39Z as Test Admin, the table showed both actions and matching actor/target IDs and timestamps, correlating to events `7016a266-22b5-4419-ac34-a64741f780df` and `ea61ab25-36bb-483e-a18c-c83cda658684`; however, the revoke Details cell was blank because the page ignored `old_value`. Initial screenshot `/private/tmp/myk9-211-evidence/permission-audit-1440x900.png`, SHA-256 `60a4fd9733370645aacd42876b9b3c0411194a0778f46099c8d33e139c9681bf`, records the exposed defect rather than passing evidence.
- [x] 3.3 Add assertion-first regression coverage and fix the audit table/search/export details fallback from `new_value` to `old_value`. — The new revoke-row assertion failed before implementation and passed 9/9 afterward. A local 1440x900 browser replay against the live audit rows showed exact role/scope details for both grant and revoke with zero console errors; private screenshot `/private/tmp/myk9-211-evidence/local-audit-revoke-details-1440x900.png`, SHA-256 `1a5afb8876f704ca5dbdd73b72ae775d1efb575c0b61d154c510d75d71f6c72d`.
- [x] 3.4 Re-run the focused RoleManager audit, AuditLogger, RecentAccessChanges, and PermissionAuditPage tests under six shuffled seeds, plus app-local typecheck/lint, `git diff --check`, and strict OpenSpec validation. — Four files / 35 tests passed under shuffled seeds 1–6; the audit-page test also passed after the final simplification. `@myk9/show` typecheck and lint, `git diff --check`, and strict OpenSpec validation all passed.
- [x] 3.5 Complete an independent Codex security/evidence review and resolve all blocking findings before shipping. — The first review exposed the blank revoke Details cell and incomplete deployment correlation; both were corrected. Final independent re-review passed with no blocking or follow-up findings and explicitly approved commit/push/PR, while retaining task 3.6 as the post-deploy closure gate.
- [x] 3.6 After merge and successful production deployment, re-walk `/admin/permissions?tab=audit` at 1440x900, verify the revoke row displays exact role/scope details, and replace the defect screenshot/checksum with passing staging evidence before closing MYK9-211. — PR #1781 squash-merged as `f928a5983f4ab6e02b2566aebcfd28888c7d5102`. Production deployment `6071223011` / successful status `17269755739` binds that SHA to `https://myk9-platform-myk9show-85780ufk1-richard-beezleys-projects.vercel.app`. At 2026-08-24T20:54:37Z, the production alias at 1440x900 as Test Admin displayed exact secretary/Heartland details for both grant and revoke with zero console errors. Final private screenshot `/private/tmp/myk9-211-evidence/staging-audit-revoke-details-f928a5983-1440x900.png`, SHA-256 `9b3d7d4b8c1fd099e5a5165f0a04aebd6a7f118ad4162cf2d304eea7574fd8e5`.

## 4. Tracking and shipping

- [x] 4.1 Update `docs/plan-linear-backlog-batches.md` with the exact mutation, cleanup, browser, and test evidence; do not embed credentials or private screenshots.
- [x] 4.2 With explicit Linear-write approval, move MYK9-211 to In Progress and post the named-fixture execution note; after proof, post event IDs/timestamps, checksums, cleanup, risks, and acceptance status. — Linear contains the corrected baseline, applied proof, exposed-defect correction, PR link, merge/deployment identity, final screenshot checksum, cleanup read-back, and passing acceptance statement. MYK9-211 is Done.
- [x] 4.3 Commit locally, then with explicit GitHub-write approval push the single verification branch and open one PR containing `Tracked in openspec change: myk9-211-applied-rbac-audit-proof`; pass CI and the Codex gate, and merge from the primary checkout only with explicit merge approval. — PR #1781 passed all CI/Vercel checks and both Codex gates, then squash-merged from the primary checkout as `f928a5983f4ab6e02b2566aebcfd28888c7d5102` after explicit approval.
- [x] 4.4 After the implementation PR is merged and Linear evidence is complete, move MYK9-211 to Done and prepare the completed OpenSpec change for the repository archive gate. — MYK9-211 is Done with final evidence; the change is archived at `openspec/changes/archive/2026-08-24-myk9-211-applied-rbac-audit-proof`; archive-only PR #1782 carries the final tracker and archive move.

### Post-archive repository hygiene

After PR #1782 merges, prune remote refs, delete the local MYK9-211 implementation and archive branches, and remove this worktree as the final command. The primary checkout's unrelated local `docs(test-accounts)` commit and research file must remain untouched.

## Validation Profile

- Risk: high
- Validation: applied staging proof plus focused regression checks
- Rationale: The shipped code already passed full implementation CI, but closure requires an authorized RBAC mutation and permanent audit evidence in the shared staging system.
