## 1. Date formatting: audit and migrate

- [x] 1.1 Grep for every remaining call site of the ~9 duplicate `formatDate`-style functions (`packages/core/src/utils/dateFormatting.ts`, `apps/myk9show/src/utils/{dateFormat,dateLocal,date-format,entryManagementUtils}.ts`, `pages/secretary/{WaitlistManagementPage/utils.ts,ResultsSubmissionPage/helpers.ts}`, `components/sync/conflict-resolution-utils.ts`, `lib/utils.ts`) and list them per file.
- [x] 1.1a [ADDED, CORRECTED] Catalogue each function's actual behavior (not just its name) before migrating. Confirmed: `entryManagementUtils.formatDate`/`WaitlistManagementPage/utils.formatDate`/`lib/utils.formatDate` are genuine short-date duplicates (`"Jul 3, 2026"`); `dateLocal.formatDateDisplay` duplicates the numeric-MMDDYYYY family; `conflict-resolution-utils.formatDate` is a relative-time formatter (not a duplicate, excluded); `dateLocal.formatDateLocal` is Date→string serialization (not a duplicate, excluded); `WaitlistManagementPage.formatDateTime` and `ResultsSubmissionPage.formatDate` are independently-invented date+time formatters (real fragmentation, but a distinct concept needing a new canonical helper, not the short-date one). Fallbacks found: `'TBD'` (Waitlist), `'Unknown'`/`'Unknown time'` (Waitlist/conflict-resolution), `''` (dateLocal), `'Invalid Date'` (lib/utils), raw passthrough (entryManagementUtils has none — no-date is impossible for `created_at`).
- [x] 1.2 Added `formatShortDate` (`"Jul 3, 2026"`, no weekday) and `formatEntryDateTime` (date + time, `"Jul 3, 2:00 PM"`) to `lib/format/dates.ts`, documented in its catalogue docblock, both rendering `''` on empty/unparseable input per the existing convention.
- [x] 1.3 Migrated the 3 genuine short-date duplicates onto `formatShortDate`: `EntriesTab.tsx` (was `entryManagementUtils.formatDate`, `|| 'TBD'` fallback preserved at call site), `ShowClassSelection.tsx` (was `WaitlistManagementPage/utils.formatDate`, `|| 'TBD'` preserved, both usages), `MoreFromClub.tsx` (was `lib/utils.formatDate`).
- [x] 1.3a [CORRECTED during implementation] `dateLocal.ts formatDateDisplay` turned out to have **zero call sites anywhere in the app** — the two files I'd flagged (`EditTrainingEntryDialog.tsx`, `PedigreeAncestorForm.tsx`) actually import `formatDateLocal` (the Date→string serializer), not `formatDateDisplay`. Verified via repo-wide grep. Deleted `formatDateDisplay` from `dateLocal.ts` and its re-export in `dateFormat.ts` outright (dead code, not a migration) — see 5.2 for the still-unused `@myk9/core` copy left in place pending a decision on that package's public API.
- [x] 1.3b Migrated `WaitlistManagementPage`'s `formatDateTime` (used in `WaitlistTable.tsx`, `|| 'Unknown'` fallback preserved) and `ResultsSubmissionPage/helpers.ts formatDate` (used in `ResultsSubmissionPage/index.tsx`, raw-passthrough fallback preserved) onto `formatEntryDateTime`.
- [x] 1.4 Deleted `entryManagementUtils.formatDate`, `lib/utils.formatDate`, `ResultsSubmissionPage/helpers.formatDate`, `dateLocal.formatDateDisplay`; deleted the now-fully-empty `WaitlistManagementPage/utils.ts` file entirely. Confirmed via repo-wide grep that zero references remain to any of them. Left `conflict-resolution-utils.formatDate` and `dateLocal.formatDateLocal` untouched — confirmed not duplicates (see 1.1a).
- [x] 1.5 Audited all 7 per-landing-theme date files (`gazette`, `heritage`, `magazine`, `headline`, `fieldGuide`, `banner`, `poster`). Every one takes an explicit `timezone` IANA-string parameter and applies it via `Intl.DateTimeFormatOptions.timeZone`; `fieldGuide`'s own docblock documents the pattern as an intentional self-contained sibling (not accidental duplication). No non-timezone-specific logic found — nothing to migrate.
- [x] 1.5a Ran `pnpm typecheck` across the monorepo after this batch — all 26 packages/apps pass, zero stray imports.
- [x] 1.6 [ADDED after scope review] Deduplicate `formatDateMMDDYYYY`: `apps/myk9show/src/utils/dateFormat.ts` now re-exports the one from `@myk9/core` instead of redefining it. All 16 call sites import from `@/utils/dateFormat` and are unchanged.
- [x] 1.7 [ADDED after scope review] Confirmed `apps/myk9show/src/utils/date-format.ts` (`formatDateRange`, `toLocalDate`, `toLocalDateOnly`) is infrastructure `lib/format/dates.ts` imports — left untouched.

## 2. Status display: audit and migrate

- [x] 2.1 Read `services/entryDisplay/entryDisplaySelectors.ts` (the classifier) and `entryStatusUiAdapter.ts` (the UI-enum projection) in full. Found the "Pending Review" wording lives in `pages/MyEntriesPage/modules/myEntriesUtils.tsx:45`, not in `EntryRow.tsx` as the proposal assumed (see 2.2).
- [x] 2.1a Grepped `docs/INTENT.md`-relevant files for `// INTENT:` comments before editing — none found, confirmed clean.
- [x] 2.2 [CORRECTED during implementation] `components/exhibitor/EntryRow.tsx` and its `STATUS_BADGE_STYLES`/`STATUS_LABELS`/`DashboardEntry` type are **unused dead code** — zero importers anywhere in the app (verified via repo-wide grep for `from '@/components/exhibitor/EntryRow'`, `DashboardEntry`, and the barrel re-export chain). Its sibling `ResultRow.tsx` (which already correctly uses the shared `ResultBadge` component, no map of its own) is equally unused, kept alive only by the barrel `index.ts` re-export. Nothing to migrate — there is no live surface showing this status map today. Logged for `OPEN-TODOS.md` (5.2) as a separate dead-code-removal candidate; not deleted here since removing whole unused components is outside this change's date/status-consistency scope.
- [x] 2.3 [CORRECTED during implementation] `utils/entryStatusUtils.ts`'s `getEntryStatusBadgeStyle`/`EntryStatus`/`getEntryStatus` classify a **show's registration-window state** (not-yet-open / accepting / closing-soon / closed / submitted) — a different domain from `entryDisplaySelectors.ts`'s per-entry lifecycle classifier (pending/accepted/withdrawn/etc.). They don't overlap and there is no existing shared classifier for the registration-window domain, so there is nothing to migrate this onto; it is not a duplicate. Notably, `getEntryStatus`'s date-derived labels (`"Closes in N days"`, `"Entries Closed"`) already recompute from live date math on every call — this is the *correct* pattern the section 3 bug (task 3.1) is missing, not an instance of the bug.
- [x] 2.4 Evaluated `packages/ui/src/components/StatusBadge`: it's a generic, expressive-enough component (label + variant + optional icon/time) that could technically represent Scored/Editing/Pending, but it renders as a rounded-pill with a different color palette than the shadcn `<Badge>` `ClassResultsTable/StatusBadge.tsx` currently uses — switching would be a visible redesign, which design.md's Non-Goals explicitly rule out for this pass. **Decision: leave `ClassResultsTable/StatusBadge.tsx` as-is.** This resolves design.md's Open Question as "no migration" rather than "yes."
- [x] 2.5 N/A — no code changed in section 2 (all three findings were either dead code, a non-duplicate distinct domain, or a documented decision not to migrate), so there is nothing new to visually verify. Existing exhibitor My Entries / Show Details tab / secretary Entry Management behavior is unchanged.

## 3. Date/status mismatch fix

- [x] 3.1 [CORRECTED] Fixed `features/monogram/landing/sections/FinalCtaBand.tsx` and `HeroBlock.tsx` using `useCountdown(entryCloseDate, timezone).closed` (not `canEnterOnline` — see design.md correction) to gate the "Closes {date}" label. `HeroBlock`'s always-visible meta cell now shows "Closed" instead of "TBA" once past close; `FinalCtaBand`'s conditionally-rendered line disappears entirely (matching its pre-existing pattern for a missing date).
- [x] 3.2 [EXPANDED — confirmed systemic, not isolated] Audited all 6 sibling themes. `heritage/HeroBlock.tsx` already gates correctly on `closed` (the reference-correct pattern). The other 5 had the identical unconditional bug and are now fixed: `gazette/FinalCtaSection.tsx`, `magazine/FinalEditorialBand.tsx`, `fieldGuide/FinalCtaSection.tsx`, `banner/FinalFlagBand.tsx` (the exact file the original research cited), `poster/{FinalCtaSection,HeroBlock}.tsx`. Also found and fixed 2 more instances in `headline` theme (`HeadlineLandingPage.tsx`'s hero meta line, `HeadlineLandingLowerSections.tsx`'s `FinalCta`) — headline already computed `countdown` for its ticker but never gated the adjacent "Closes"/"Closing" text with it. Deliberately left untouched: each theme's `ParticularsSection.tsx`/detail-table rows and `headline`'s capacity-bar footer, which show entry-window dates as historical/reference facts in a spec table, not as an active "still open" claim — hiding them after close would make the reference table incomplete, not more correct.
- [x] 3.3 Verified via `pnpm typecheck` (zero errors across all 8 touched files) — no dev server available in this environment for a live click-through; see 4.5 for the deferred manual/browser verification step.

## 4. Tests

- [x] 4.1 Extended `apps/myk9show/src/lib/format/__tests__/dates.test.ts` with `formatShortDate` and `formatEntryDateTime` describe blocks (8 new tests), matching the file's existing style. All 24 tests in the file pass.
- [x] 4.2 [N/A, per 2.2/2.3 corrections] No status-map migration happened in section 2 (dead code / different domain / documented no-migrate decision), so there is nothing to assert equivalence against. Existing `entryStatusUtils.test.ts` (unchanged) still passes.
- [x] 4.3 [CORRECTED to `countdown.closed`, not `canEnterOnline`] Added gating tests to `monogram/landing/__tests__/entryCtas.test.tsx` (HeroBlock: closed→"Closed" not stale date, open→date shown, no-date→"TBA") and `banner/landing/__tests__/bannerEnterCtaGating.test.tsx` (FinalFlagBand: closed→line hidden, open→line shown, no-date→line hidden/fails open). 17 tests, all pass.
- [x] 4.4 Ran the full myk9show suite: `npx vitest run` on all touched directories (landing themes: 79 files/698 tests; date/status/entry-management: 17 files/159 tests; new gating tests: 2 files/17 tests) — zero regressions, zero failures.
- [x] 4.5 [CORRECTED — preview tools were available] Verified live via the dev server + Preview browser tools against a real published show (`Heartland Scent Work Classic`, `dededede-0000-0000-0000-000000000010`, style `monogram`). Open state: hero meta grid showed "CLOSES / Aug 31" and the countdown ticker rendered correctly. After temporarily setting `entry_close_date` to a past date through the app's own Edit-Show UI (signed in as `e2e-secretary@test.myk9.com`) and republishing: hero meta grid correctly showed "CLOSES / Closed" (not a stale date), and `FinalCtaBand`'s "Closes {date}" line disappeared entirely — both confirmed via accessibility-tree snapshot of the anonymous (signed-out) public view. **Revert needed**: the temporary edit could not be fully reverted through the app UI — its own validation rejects `entry_close_date` after `start_date` (the original seeded value, `2026-09-01`, is after the show's `2026-08-01` start, so the seed itself predates this rule). Current DB state on that show: `entry_close_date=2026-07-01`, `experience_is_published=true`, `experience_published_style='monogram'` (originals: `2026-09-01`, `false`, `null`) — user opted to handle this revert directly rather than via a service-role write.

## 5. Wrap-up

- [x] 5.1 `pnpm typecheck` clean across all 26 workspace packages/apps. `pnpm lint` initially caught one irregular-whitespace error (a literal U+202F character in `formatEntryDateTime`'s regex instead of the escaped ` ` form `formatTime` uses) — fixed; lint now clean.
- [x] 5.2 Logged to `OPEN-TODOS.md`: (1) `EntryRow.tsx`/`ResultRow.tsx` in `components/exhibitor/` are unused dead components (candidate for a separate removal PR); (2) `@myk9/core`'s `formatDateDisplay` export is now unused app-side after this change but left in the package's public API pending a decision on whether other consumers exist; (3) `RoleRequestsPage.tsx`/`PayoutLedgerPage.tsx` admin-only hardcoded status maps, out of scope here.
- [x] 5.3 [FIXED] Archived this OpenSpec change with `/opsx:archive` — not the `docs/plan-*.md` git-mv convention, which applies to `docs/` plans, not `openspec/changes/`.
