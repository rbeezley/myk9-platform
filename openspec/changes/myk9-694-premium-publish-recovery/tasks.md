## 1. Diagnose and align contract

- [x] 1.1 Isolate the premium publish failure and record the root cause.
- [x] 1.2 Identify that upload to a public bucket before commit violates committed-only download.
- [ ] 1.3 Replace the rejected public-bucket/legacy-write design with private Storage and committed-pointer downloads.
- [ ] 1.4 Validate this revised contract against MYK9-694 acceptance criteria and preserve unrelated MYK9-691 show-style behavior.

## 2. Implement private committed publication

- [ ] 2.1 Add a public download Edge Function that resolves the current committed row path server-side. Permit anonymous reads only for the canonical public-show visibility predicate/status. Preserve manager preview of committed drafts only after validating bearer identity and checking existing `can_manage_show` / `is_show_secretary` RPCs under that user JWT; service role may sign only after authorization. Preserve exact legacy flat reads without mutating old rows or bytes.
- [ ] 2.2 Replace the migration with one private-bucket contract: row-locked begin-or-reconcile and atomic commit RPCs, authenticated scoped insert, no client update/delete for versioned objects, PDF/size constraints, explicit grants, schema reload, `public=false`, and an assertion that no broad anon-read policy remains. Reconcile only an exact prior committed version/path; otherwise reserve a new version. Enforce one publication-state boundary on both show INSERT and UPDATE: non-Postgres inserts must start at safe defaults (null pointer/URL/timestamp, attempt version zero, committed version null, experience unpublished with null timestamp/style and `{}` snapshot); all later publication-owned changes are RPC-only, with no service-role exception. Audit and prevent generic replicated-show updates from submitting these fields.
- [ ] 2.3 Remove missing-schema legacy publishing and the sticky `legacySchemaByShowId` cache; missing RPC remains retryable and the next publish rechecks it.
- [ ] 2.4 Route every publisher entry point through one exported `runPremiumPublishOperation` that installs a synchronous per-show lock, shares an identical mode/key promise, returns a typed conflict for a different intent, and allows other shows to proceed. Keep the editor's authored unsaved premium draft and route it through this coordinator. Generated retries reconcile their exact persisted commit before invoking the nondeterministic generator; an uncommitted retry then reserves fresh, regenerates current source data, and reuses staged bytes only when mode, intent key, complete content, and publisher match. Draft retries reconcile only the exact prior draft.
- [ ] 2.5 Keep all retained Storage objects/rows intact; do not alter Darboshea.

## 3. Verification

- [ ] 3.1 Client tests cover staged upload privacy assumptions, missing-schema behavior, subsequent retry after migration, lost-response reconciliation without another generation/upload, identical/changed generated intent, publisher changes, exact/changed authored draft retries, cross-surface conflicts, same-intent promise sharing, independent shows, stale attempts, clear success, and actionable/generic failure states. An uncommitted retry reserves fresh before regeneration; identical content/account reuses the immutable artifact under the new version, while changed intent gets a new artifact. Durable readiness remains available during signing outages; signed links are minted freshly only on explicit Download and are not retained in query/persistent state.
- [ ] 3.2 Edge Function and client metadata tests cover pre-migration schema (legacy URL available without `published_premium_path`) and post-migration schema, exact missing-column fallbacks for both `PGRST204` and `42703`, unrelated-error propagation, committed versioned downloads, exact legacy/Darboshea reads, published anonymous access, anonymous draft denial, invalid-token denial, unrelated-manager denial, authorized manager/secretary/admin draft preview, arbitrary/other-show/superseded path rejection, missing objects, and no-pointer behavior.
- [ ] 3.3 Behavioral SQL covers authorization, exact staging paths, PDF/size constraints, immutability, safe-default authenticated show creation, denial of prefilled direct authenticated publication-state INSERTs, direct authenticated UPDATE denial for every publication-owned show field, atomic commit/failure preservation, stale versions, same-version exact-intent idempotence versus changed-style/snapshot rejection, private bucket/no broad anon-read policy, and grants; repair fixture teardown at the failing direct `DELETE FROM storage.objects` by cleaning with the test transaction/owner context, never by weakening `storage.protect_delete` or published-object immutability.
- [ ] 3.6 Show Desk reads the durable `usePublishInfo` shape (`publishedLocator`/`hasPublishedPremium`) and does not classify an existing publication as unpublished; keep download signing on explicit click only.
- [ ] 3.4 Integration check proves direct anonymous GET of staged bytes fails, committed endpoint download succeeds, failed republish preserves the old download, and Darboshea's row/object are unchanged.
- [ ] 3.5 Run OpenSpec strict validation and the narrowest relevant checks; execute registered behavioral SQL in CI and record its result.

## 4. Integrate and deliver

- [ ] 4.1 Coordinate sequentially with PR #2377, which overlaps `showMappers.ts`, `ShowDetailsPage.test.tsx`, and behavioral SQL registry files. Rebase/integrate only after #2377 lands; retain its MYK9-691 style behavior and inspect the combined diff.
- [ ] 4.2 Complete required review and CI on the final head; resolve findings before merge.
- [ ] 4.3 After final review and CI pass, obtain separate approval and deploy the backward-compatible download Edge Function from the reviewed PR head; verify legacy reads and non-public show denial. Merge next; Vercel auto-deploys the frontend immediately, before CI finishes. Until migration, old clients/direct links continue to work and the new app shows retryable missing-RPC setup guidance without legacy writes; disclose that legacy uploaded bytes remain fetchable before row commit during this interval. Obtain separate linked-database approval and apply migration promptly; then verify fresh publish, private staging, committed-pointer downloads, production build, and unchanged/readable Darboshea. After cutover, old-client writes/direct links are unsupported and recovery is forward-only. Close MYK9-694 only after these evidence gates pass.
