## Why

Five confirmed Todo defects from the September secretary journey audit block or undermine routine setup, report accuracy, keyboard access, and repeatable QA. Resolving them now supports fall 2026 launch readiness by making the secretary path calm, operable, and financially trustworthy before launch.

## What Changes

- Keep cloned non-template classes visible, searchable, editable, and removable in the existing show-creation Classes step.
- Replace the browser-only `require` failure in Create Classes with the existing template-store API, and make template cards keyboard operable with a programmatic selected state.
- Align the secretary audit skill and scheduled prompt with the canonical fixture resolver instead of a deleted hardcoded account or contradictory domain ban.
- Give club show action menus show-specific accessible names, 44px touch targets, and keyboard behavior that does not trigger parent-card navigation.
- Resolve AKC Trial Secretary fees by event year, share one runs-paid calculation between preview and PDF, and remove or align stale duplicate report instructions.
- Add focused unit and interaction coverage plus multi-viewport browser evidence for each affected workflow.

No new page, dialog, report, or workflow surface is introduced. These defects live inside existing canonical surfaces; a link would not repair hidden cloned state, broken selection, incorrect report math, inaccessible controls, or audit configuration drift. Non-goals include rebuilding alternate show/class flows, changing authentication or fixture accounts, adding a third report format, submitting remittances, or modifying hosted data.

## Capabilities

### New Capabilities

- `secretary-class-configuration-integrity`: Existing show and class creation surfaces preserve reachable cloned state and provide operable template selection.
- `secretary-audit-fixture-resolution`: Scheduled secretary audits resolve one canonical configured fixture and report precise coverage gaps without exposing secrets.
- `club-show-card-interaction`: Existing club show cards distinguish card navigation from accessible action-menu activation.

### Modified Capabilities

- `registry-closeout-verification`: Trial Secretary preview and downloadable output use the same year-aware fee and eligible-run policy.

## Impact

- myK9Show show-creation wizard, class-creation stores/components, club details show cards, Trial Secretary preview/PDF builders, and focused tests.
- Repository audit skills and the existing weekly secretary automation prompt/configuration.
- No database migration, persistent-data mutation path, API, RLS, payment submission, or replication contract changes. Existing replication-backed reads remain unchanged.
