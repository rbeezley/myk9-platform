# Live-data evidence

On 2026-09-19, a read-only query against the linked Supabase database counted class rows whose `element`, `level`, or `section` equals the literal `Unknown`.

Result: 0 affected rows (`element`: 0, `level`: 0, `section`: 0). No data was mutated and no operator repair is required for the current dataset.
