/**
 * Re-export shim. Canonical implementation lives in
 * `features/_shared/hooks/useRevealOnScroll.ts` so every premium style shares
 * it without copy-paste. Magazine call sites import from this path.
 */
export { useRevealOnScroll } from '../../_shared/hooks/useRevealOnScroll';
