# Formatting-only commits. `git blame` skips them when configured with
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# and GitHub's blame view reads this file automatically.
# scripts/qa/edge-function-drift.ts also skips them when dating a function's
# last source edit (MYK9-597). List ONLY commits that change no behaviour.

# style: one-time Prettier pass; CI checks formatting on changed files (#2121)
d6e775f9d0094e73e8e4043f6900d48e46c2ec30
