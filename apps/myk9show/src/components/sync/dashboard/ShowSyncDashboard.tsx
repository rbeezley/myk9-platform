import { ShowSyncDashboard as MainShowSyncDashboard } from '../ShowSyncDashboard';

// Re-export the main component for backward compatibility
export const ShowSyncDashboard = MainShowSyncDashboard;
export default ShowSyncDashboard;