## 1. Fixture identity

- [x] 1.1 Choose three plausible distinct names and update `setup-e2e-test-users.ts` plus every canonical seed write; verify a tracked-source search shows no rendered “Load Secretary One/Two/Three” names.
- [x] 1.2 Update intentional test and tracked scheduled-walk name references while preserving stable emails; inspect user-local scheduled-task parity read-only and report any separately owned update, then verify focused load/seed contract tests pass.

## 2. Verification and delivery

- [x] 2.1 Run focused tests, typecheck the affected app/scripts, and run `pnpm qa:code-quality-ratchet` if existing files grow.
- [ ] 2.2 Validate the OpenSpec change, complete adversarial review with at least two Luna lenses, and address every actionable finding.
- [ ] 2.3 Open the MYK9-606 PR with evidence and confirm required CI reaches a mergeable or clearly reported state; merge only with separate authorization.

## 3. Post-merge evidence

- [ ] 3.1 After an explicitly approved reseed, verify the club roster renders the new names and record that evidence in Linear before marking Done.
