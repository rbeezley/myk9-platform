/**
 * Re-export shim. Canonical implementation lives in
 * `features/_shared/hooks/useReducedMotion.ts` so every premium style shares
 * it without copy-paste. Magazine call sites import from this path.
 */
export { useReducedMotion } from '../../_shared/hooks/useReducedMotion';
