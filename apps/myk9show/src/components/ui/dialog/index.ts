// Card-background / foreground defaults are baked into dialog.tsx itself
// (formerly the enhanced-dialog wrapper), so the barrel is a straight re-export.
export {
  Dialog,
  DialogTrigger,
  DialogPortal,
  DialogClose,
  DialogOverlay,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogDescription,
  DialogFooter,
} from './dialog';
