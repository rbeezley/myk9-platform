# Improve Audit — June 2026 (remaining plans)

> **Status:** Active

Generated by the `/improve` skill (standard effort) against commit `deb820e35` on
2026-06-21. The audit produced six plans; the two P1 quick wins have shipped, and
the four below are the remaining, self-contained, executor-ready plans.

Each plan is written for an executor with zero session context: inlined code
excerpts, exact verification commands, hard scope boundaries, and STOP
conditions. Execute one at a time; honor each plan's STOP conditions.

## Shipped (not included here — see git history)

| Plan | Title | Outcome |
|------|-------|---------|
| 001 | Index ShowDetailsPage entry counts by class (kill per-class O(n) scan) | ✅ Merged — PR [#903](https://github.com/rbeezley/myk9-platform/pull/903) |
| 002 | Extract one shared owned-dog selector across 12 call sites | ✅ Merged — PR [#910](https://github.com/rbeezley/myk9-platform/pull/910) |

## Remaining plans

| Plan | Title | Priority | Effort | Risk | Kind |
|------|-------|----------|--------|------|------|
| [003](003-calculatetotalareatime-zero-bug.md) | Fix `calculateTotalAreaTime` falsy 0-check | P2 | S | LOW | fix (contract) | ✅ Done — branch `advisor/003-area-time-zero` |
| [005](005-scoring-placement-integration-test.md) | Integration test: score → placement → exhibitor-visible rank | P1 | M | LOW | tests | ✅ Done — contract already pinned by existing `src/pages/scoring/__tests__/calculatePlacements.test.ts` (13 cases); no new file needed |
| [006](006-insert-retry-idempotency-investigate.md) | Investigate INSERT retry idempotency in the replication queue | P2 | M | LOW | investigate (read-only) — **DONE: Verdict B; findings → `docs/plan-replication-insert-idempotency.md`** |
| [004](004-entries-read-module-consolidation-spike.md) | Spike: map & rationalize the 7 entry-read modules | P2 | L | LOW | investigate (doc-only) — ✅ Complete; findings at [docs/plan-entries-read-consolidation.md](../plan-entries-read-consolidation.md) |

Suggested order: **005** (the one P1 — guards the platform's core scoring value),
then **003** (trivial), then the two read-only spikes **006** and **004** whose
output is each a follow-up plan rather than a code change.

## Notes

- **003** is honestly a contract/clarity fix, not a live bug — `0` adds nothing
  numerically. The plan says so; don't oversell it in a PR.
- **004** and **006** are spikes: each produces a `docs/` findings doc that
  *scopes* a later, riskier refactor (both touch offline-critical paths) — they
  deliberately do not change code.
- To run one with a dispatched executor + review: `/improve execute <plan-file>`.
  To tighten one first: `/improve review-plan <plan-file>`.

## Rejected findings (recorded so they aren't re-audited)

The audit's headline security findings did **not** survive vetting and were
rejected — recorded here so a future run doesn't re-surface them:

- **Anon RBAC-function grants / anon PII view** — already revoked by
  `20260613100000_security_revoke_anon_grants.sql` and migration 117.
- **Stripe Connect onboarding "CSRF"** — not real; the endpoint is JWT-header
  auth with a club-admin check, not cookie/CSRF-able.
- **MutationManager "silent data loss"** — by design (post-upload backup,
  documented in-code).
- **Replication watermark "regression"** — monotonicity is enforced via the
  `advanceWatermarkMonotonically` flag.
- **"Three competing data-fetch strategies"** — the documented Zustand / React
  Query / replication split (CLAUDE.md State Management).
