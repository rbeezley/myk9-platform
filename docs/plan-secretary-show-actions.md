# Secretary Show Actions — Inventory and Collapse Plan

> **Status:** Active

**Linear:** [MYK9-630](https://linear.app/myk9-platform/issue/MYK9-630) · **Scope:** AC 1 only (inventory, no code) · **Date:** 2026-09-17
**Method:** `IA-Review` skill (route audit → task walk → duplication scan → recommendation), plus a read-only
production walk as `secretary@myk9t.com` on `/shows/dededede-0000-0000-0000-000000000010`
(Heartland Scent Work Classic, 517 entries). 18 screenshots at 1440px and 375px, one per surface.
**Read first:** [`INTENT.md`](INTENT.md) § Trial Secretary ("That was easy" — _absorb complexity, not add to it_;
anti-pattern: "anything that makes the user feel like they need to learn the software") and
[`archive/plan-show-map-workbench-collapse.md`](archive/plan-show-map-workbench-collapse.md) (the precedent:
Today + Wrap-up were **deleted**, not rearranged — "if a section can't justify its space, it doesn't survive the collapse").

## Route audit — the secretary's surfaces for one show

| Route                                                         | What it is                                                                          | Component                                                                                              |
| ------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| `/shows/:id`                                                  | Management shell: hero + `...` menu, publish row, section nav, 6 tabs               | `pages/ShowDetailsPage.tsx` → `components/shows/ShowDetails/ShowManagementShell.tsx`                   |
| `/shows/:id/show-desk`                                        | Show-day hub: Show Map, 11-section Tools sheet, closeout                            | `pages/secretary/ShowWorkbenchShowDeskPage.tsx`                                                        |
| `/shows/:id/entry-management`                                 | Registration cockpit (queues, accept/reject, payments)                              | `pages/secretary/EntryManagementPage.tsx` + `components/entries/management/EntryManagementCockpit.tsx` |
| `/shows/:id/reports` · `/results-control` · `/submit-results` | Print, release, file                                                                | section `<Outlet/>` under the same shell                                                               |
| `/shows/:id/setup`                                            | In the registry, **not** in the nav (`SHOW_MANAGEMENT_NAV_SECTIONS` filters it out) | `routes/showManagementSections.ts:16`                                                                  |
| `/secretary/register/:showId`                                 | Mail-in wizard                                                                      | `pages/RegistrationWizardPage.routes.ts:9`                                                             |
| `/shows/:id/register`                                         | "Enter my own dogs" (exhibitor wizard)                                              | `pages/RegistrationWizardPage.routes.ts:5`                                                             |
| `/secretary/register/:showId?source=show-desk&entryMode=late` | Late entry                                                                          | `pages/RegistrationWizardPage.routes.ts:13`                                                            |

The nav row is data, not markup: `SHOW_MANAGEMENT_NAV_SECTIONS` = Show Desk · Entry Management · Reports · Results · Submit Results.
Every management section renders **inside** the same shell, so the hero, the `...` menu and the publish row repeat on all five.

## Inventory — every secretary action on a show

Paths are `apps/myk9show/src/…`. "Places" counts every surface the same capability is reachable from; each place gets its own row so the count is visible.

| Action (as the user reads it)                                                                                        | Surface                                                                 | Route                         | component:line                                                                                                     | Places         | Recommendation                                                                                                                                                                             |
| -------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------ | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Add mail-in entry                                                                                                    | Show Desk → Tools → "Add entries"                                       | `/shows/:id/show-desk`        | `features/registration/SecretaryAddEntriesDecision.tsx:42` via `pages/secretary/ShowWorkbenchShowDeskPage.tsx:295` | 2              | **move under actions menu** — the one thing the show page never offers; today it hides behind a wrench icon.                                                                               |
| Add mail-in entry                                                                                                    | Entry Management → "Add entry" popover                                  | `/shows/:id/entry-management` | `pages/secretary/EntryManagementPage.tsx:290`                                                                      | 2              | **keep** — canonical home; the header menu should link here, not re-implement.                                                                                                             |
| Enter my own dogs                                                                                                    | Show Desk → Tools → "Add entries"                                       | `/shows/:id/show-desk`        | `SecretaryAddEntriesDecision.tsx:32`                                                                               | 2              | **move under actions menu** — same pair, same reason.                                                                                                                                      |
| Enter my own dogs                                                                                                    | Entry Management → "Add entry" popover                                  | `/shows/:id/entry-management` | `EntryManagementPage.tsx:290`                                                                                      | 2              | **keep**                                                                                                                                                                                   |
| Add a late entry                                                                                                     | Show Desk → Tools → "Add entries"                                       | `/shows/:id/show-desk`        | `ShowWorkbenchShowDeskPage.tsx:296`                                                                                | 1              | **keep** — show-day-only mode; belongs at the desk, not in a general menu.                                                                                                                 |
| Open Entry Management                                                                                                | Section nav row                                                         | every `/shows/:id/*`          | `routes/showManagementSections.ts:4` rendered at `ShowManagementShell.tsx:329`                                     | 4              | **keep** — the canonical link.                                                                                                                                                             |
| Open Entry Management                                                                                                | Entries tab, empty state                                                | `/shows/:id?tab=my-entries`   | `components/shows/ShowDetails/EntriesTab.tsx:86`                                                                   | 4              | **delete** — third copy of a link that is already two inches above it in the nav row.                                                                                                      |
| Open Entry Management                                                                                                | Entries tab, populated state                                            | `/shows/:id?tab=my-entries`   | `EntriesTab.tsx:109`                                                                                               | 4              | **delete** — the whole tab is a count plus this button; the count is already on the tab label.                                                                                             |
| Open Entry Management (4 filtered presets)                                                                           | Command palette, contextual                                             | ⌘K                            | `features/command-menu/contextualCommands.ts:32`                                                                   | 4              | **keep** — but it only registers when `ctx.surface === 'entry-management'` (`contextualCommands.ts:23`), i.e. it is unreachable from the show page, which is where you would reach for it. |
| Open Show Desk                                                                                                       | Section nav row                                                         | every `/shows/:id/*`          | `showManagementSections.ts:3`                                                                                      | 3              | **keep**                                                                                                                                                                                   |
| Open Show Desk                                                                                                       | Sidebar "next show" item (today-phase shows)                            | global                        | `components/layout/sidebar/unifiedSidebarConfig.ts:216`                                                            | 3              | **keep** — cross-show entry point, a different job.                                                                                                                                        |
| Open Show Desk                                                                                                       | Entry Management → "More" → Open Check-in desk (`?tool=people-at-show`) | `/shows/:id/entry-management` | `EntryManagementPage.tsx:262`                                                                                      | 3              | **keep** — a deep link with a tool pre-opened; the pattern the rest of this plan wants more of.                                                                                            |
| Open Reports / Results / Submit Results                                                                              | Section nav row                                                         | every `/shows/:id/*`          | `showManagementSections.ts:5-7`                                                                                    | 3 each         | **keep**                                                                                                                                                                                   |
| Results / Reports / Submit results (3 big buttons)                                                                   | Show Desk → Tools → Show closeout                                       | `/shows/:id/show-desk`        | `ShowWorkbenchShowDeskPage.tsx:379`, `:385`, `:391`                                                                | 3 each         | **delete** — an exact restatement of the nav row that is on screen at the same time.                                                                                                       |
| Go to Submit Results                                                                                                 | Results page readiness panel                                            | `/shows/:id/results-control`  | results readiness card                                                                                             | 3              | **keep** — a sequencing hand-off, not navigation chrome.                                                                                                                                   |
| Open Reports                                                                                                         | Submit Results closeout guidance                                        | `/shows/:id/submit-results`   | submit-results guidance card                                                                                       | 3              | **keep** — same reason.                                                                                                                                                                    |
| Generate & publish premium                                                                                           | Publish row (both cards render on _every_ section)                      | every `/shows/:id/*`          | `features/premium/PremiumDownloadCard.tsx:64` via `ShowManagementShell.tsx:292`                                    | 1 (× 5 routes) | **move under actions menu** — a pre-show action taking permanent hero space on Reports, Results and Submit Results.                                                                        |
| Copy public landing link                                                                                             | Publish row → Landing Page card                                         | every `/shows/:id/*`          | `features/premium/LandingPageCard.tsx:79`                                                                          | 1 (× 5 routes) | **move under actions menu** — same.                                                                                                                                                        |
| Edit show                                                                                                            | Header `...` menu                                                       | `/shows/:id`                  | `ShowManagementShell.tsx:232`                                                                                      | 2              | **keep** — already in the menu this plan wants to grow.                                                                                                                                    |
| Edit show                                                                                                            | Show Desk compact `...` menu                                            | `/shows/:id/show-desk`        | `components/shows/ShowDetails/ShowDeskCompactContext.tsx:161`                                                      | 2              | **keep** — deliberate compact chrome (`// INTENT:` at `ShowManagementShell.tsx:284`); keep the two menus _identical_.                                                                      |
| Delete show                                                                                                          | Header `...` menu                                                       | `/shows/:id`                  | `ShowManagementShell.tsx:239`                                                                                      | 2              | **keep**                                                                                                                                                                                   |
| Delete show                                                                                                          | Show Desk compact `...` menu                                            | `/shows/:id/show-desk`        | `ShowDeskCompactContext.tsx:166`                                                                                   | 2              | **keep**                                                                                                                                                                                   |
| Preview as exhibitor                                                                                                 | Header `...` menu                                                       | `/shows/:id`                  | `ShowManagementShell.tsx:226`                                                                                      | 2              | **keep**                                                                                                                                                                                   |
| Preview as exhibitor                                                                                                 | Show Desk compact `...` menu                                            | `/shows/:id/show-desk`        | `ShowDeskCompactContext.tsx:155`                                                                                   | 2              | **keep**                                                                                                                                                                                   |
| Change show status (draft → published → …)                                                                           | Status pill                                                             | every `/shows/:id/*`          | `components/shows/ShowStatusPill.tsx:184`                                                                          | 2              | **keep** — a state control, not an action; must stay visible.                                                                                                                              |
| Armband lookup                                                                                                       | Page header / desk chrome                                               | `/shows/:id`, `/show-desk`    | `ShowManagementShell.tsx:188`, `ShowDeskCompactContext.tsx:139`                                                    | 2              | **keep**                                                                                                                                                                                   |
| Retry entry counts                                                                                                   | Shell banner + every gated tab panel                                    | `/shows/:id`                  | `ShowManagementShell.tsx:271`, `ShowDetailTabs.tsx:72`                                                             | 2              | **keep** — an error affordance, correctly co-located.                                                                                                                                      |
| New Trial                                                                                                            | Trials tab toolbar                                                      | `/shows/:id?tab=trials`       | `components/shows/tabs/TrialsTab.tsx:172`                                                                          | 2              | **move under actions menu** — creation belongs with the other setup verbs, not on a browse tab.                                                                                            |
| New Trial                                                                                                            | Trials tab empty state                                                  | `/shows/:id?tab=trials`       | `TrialsTab.tsx:185`                                                                                                | 2              | **keep** — an empty state may keep its own primary action.                                                                                                                                 |
| New Class                                                                                                            | Classes tab toolbar                                                     | `/shows/:id?tab=classes`      | `components/shows/tabs/ClassesTab.tsx:297`                                                                         | 2              | **move under actions menu** — same; both jump into the same `create-show/wizard?mode=…`.                                                                                                   |
| New Class                                                                                                            | Classes tab empty state                                                 | `/shows/:id?tab=classes`      | `ClassesTab.tsx:258`                                                                                               | 2              | **keep**                                                                                                                                                                                   |
| Export classes CSV · Columns · Density · Reset view                                                                  | Classes tab table toolbar                                               | `/shows/:id?tab=classes`      | shared `DataTable` toolbar                                                                                         | 1              | **keep** — table chrome.                                                                                                                                                                   |
| View all classes                                                                                                     | Overview → Classes offered card                                         | `/shows/:id`                  | `components/shows/tabs/ShowOverviewTab.tsx:92`                                                                     | 1              | **keep** — an in-page jump, cheap.                                                                                                                                                         |
| Share this event                                                                                                     | Overview                                                                | `/shows/:id`                  | `ShowOverviewTab.tsx:107`                                                                                          | 1              | **keep**                                                                                                                                                                                   |
| Show access codes (view / regenerate)                                                                                | Overview                                                                | `/shows/:id`                  | `ShowOverviewTab.tsx:109`                                                                                          | 2              | **link** — the Show Desk copy is the show-day one; Overview should link to it rather than mount a second regenerate control.                                                               |
| Show access codes (view / regenerate)                                                                                | Show Desk → Tools                                                       | `/shows/:id/show-desk`        | `ShowWorkbenchShowDeskPage.tsx:345`                                                                                | 2              | **keep**                                                                                                                                                                                   |
| People at show / check-in lookup                                                                                     | Show Desk → Tools                                                       | `/shows/:id/show-desk`        | `ShowWorkbenchShowDeskPage.tsx:254`                                                                                | 2              | **keep**                                                                                                                                                                                   |
| Self check-in on/off                                                                                                 | Show Desk → Tools                                                       | `/shows/:id/show-desk`        | `ShowWorkbenchShowDeskPage.tsx:270`                                                                                | 1              | **keep**                                                                                                                                                                                   |
| Emergency trial packet                                                                                               | Show Desk → Tools                                                       | `/shows/:id/show-desk`        | `ShowWorkbenchShowDeskPage.tsx:301`                                                                                | 1              | **keep** (Reports already links to it in prose)                                                                                                                                            |
| Judge hospitality · Incident log · Schedule slip · Volunteers · Tasks & notes                                        | Show Desk → Tools                                                       | `/shows/:id/show-desk`        | `ShowWorkbenchShowDeskPage.tsx:308`, `:314`, `:332`, `:358`, `:365`                                                | 1 each         | **keep** — the drawer the collapse precedent created; do not promote any of these.                                                                                                         |
| Show closeout · Close out show                                                                                       | Show Desk → Tools                                                       | `/shows/:id/show-desk`        | `ShowWorkbenchShowDeskPage.tsx:370`, `:398`                                                                        | 1              | **keep**                                                                                                                                                                                   |
| Class ops (mark started, set expected start, move up, pull, collect signature, prepare labels/check-in/score sheets) | Show Desk → Show Map rows                                               | `/shows/:id/show-desk`        | `features/show-map/ShowDeskPanel`                                                                                  | 1              | **keep** — the operational hub; explicitly _not_ duplicated on the show page's map (`ShowDetailTabs.tsx:181` `// INTENT:`).                                                                |
| Review / Accept / Reject registration, comp, edit entry, assign armband                                              | Entry Management cockpit detail                                         | `/shows/:id/entry-management` | `EntryManagementCockpit.tsx`                                                                                       | 1              | **keep**                                                                                                                                                                                   |
| Queue filter (Needs review / Missing info / Payment due / All)                                                       | Entry Management cockpit                                                | `/shows/:id/entry-management` | `EntryManagementCockpit.tsx:218`                                                                                   | 1              | **keep**                                                                                                                                                                                   |
| Search · Trial/Class filter · Density                                                                                | Entry Management cockpit                                                | `/shows/:id/entry-management` | `EntryManagementCockpit.tsx:265`, `:274`, `:247`                                                                   | 1              | **keep**                                                                                                                                                                                   |
| Copy view link · Export Full CSV                                                                                     | Entry Management → "More"                                               | `/shows/:id/entry-management` | `EntryManagementPage.tsx:268`, `:270`                                                                              | 1              | **keep**                                                                                                                                                                                   |
| Exceptions (move-ups / pulls / waitlist)                                                                             | Entry Management page tab                                               | `/shows/:id/entry-management` | `EntryManagementPage.tsx:43`                                                                                       | 1              | **keep**                                                                                                                                                                                   |
| Add New Show                                                                                                         | Command palette → Actions                                               | ⌘K                            | `components/common/CommandPalette.tsx:265`                                                                         | 1              | **keep** — cross-show, not show-scoped.                                                                                                                                                    |
| Show Management (cross-show home)                                                                                    | Sidebar → Manage                                                        | global                        | `unifiedSidebarConfig.ts:206`                                                                                      | 1              | **keep**                                                                                                                                                                                   |

**Count:** 12 capabilities are reachable from 2+ places; the section-nav quintet is restated in three
places at once on Show Desk. The one capability with **zero** reachable entry point from `/shows/:id` is
the thing the secretary opens the page to do: **add an entry**.

## What the page says that is not true for a secretary

| #   | What it says                                                                                                 | Where                                                                       | Why it is wrong                                                                                                                                                                                                                                                      |
| --- | ------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Tab badge **"Entries 517"** beside a body reading **"No Entries Yet"**                                       | `/shows/:id?tab=my-entries`                                                 | Two reads. The badge is `catalogEntryCount` off `useSecretaryShowEntriesQuery` (`ShowDetailsPage.tsx:111`); the body is `EntriesTab`'s own `getEntriesByShow` (`EntriesTab.tsx:31`), which returned empty. **Reproduced on production, 2026-09-17, both viewports.** |
| 2   | "Entries will appear here once **exhibitors register** for this show."                                       | `EntriesTab.tsx:83`                                                         | Exhibitor copy shown to the person whose job is to type the entries in. It should name the action: "No entries yet. Add a mail-in entry."                                                                                                                            |
| 3   | Deep-linking `/shows/:id?tab=my-entries` renders **"Failed to load component"**                              | `/shows/:id?tab=my-entries`, cold load                                      | Reproduced at 1440px and 375px on production with **no console errors**. Clicking the tab from Overview works. Not in MYK9-630's scope — worth its own issue.                                                                                                        |
| 4   | The tab is labelled **"Entries"** but its id and URL are `my-entries`                                        | `ShowDetailsPage.tabDefs.ts:75`, `allowedTabs` at `ShowDetailsPage.tsx:236` | The manager tab shares an id with the exhibitor's "My Entries" tab, so a shared/bookmarked URL reads as the secretary's own entries.                                                                                                                                 |
| 5   | Show page says **517 entries**; Entry Management's queue says **"All registrations 514"**                    | `/shows/:id` vs `/shows/:id/entry-management`                               | 514 + the 3 in Needs review = 517, so "All registrations" is not all registrations. Two numbers for one show, one screen apart.                                                                                                                                      |
| 6   | "Premium PDF is not published yet" + "Generate & publish premium" on **Reports, Results and Submit Results** | `ShowManagementShell.tsx:287-295`                                           | A pre-show setup nag occupying hero space on three post-show surfaces; on Show Desk it appears twice (the compact publish exception at `ShowDeskCompactContext.tsx:178` _and_ the row).                                                                              |
| 7   | Show Desk attention list renders "Check in 504 entries" and "Review 3 entries" **three times each**          | `/shows/:id/show-desk`, both viewports                                      | Seen in the extracted text at 1440 and 375; likely the responsive two/three-copy antipattern rather than three real items. Needs a DOM check before it is called a bug.                                                                                              |
| 8   | Setup is a live route with **no link anywhere** in the section nav                                           | `showManagementSections.ts:16`                                              | Deliberate (the comment says so) but the sidebar's next-show item still points at `/shows/:id/setup` (`unifiedSidebarConfig.ts:217`), so a pre-show secretary lands on a page the nav then refuses to name.                                                          |

## Proposed shape — one actions surface

Grow the existing header `...` menu (`ShowManagementShell.tsx:211`) into the show's one actions menu, keep
`ShowDeskCompactContext.tsx:147` byte-identical to it, and leave the section nav row as navigation.
Eight items, in this order:

1. **Add mail-in entry** → `/secretary/register/:showId`
2. **Enter my own dogs** → `/shows/:showId/register`
3. **Open Entry Management** → `/shows/:showId/entry-management`
4. **Open Show Desk** → `/shows/:showId/show-desk`
5. _(separator)_ **Generate & publish premium** → opens the premium flow
6. **Copy public landing link**
7. **Edit show** · **Preview as exhibitor**
8. **Delete show** (destructive, last)

Deleted by this shape: both Entry Management buttons inside `EntriesTab`, the Results/Reports/Submit-results
triple in Show Desk closeout, and the always-on publish row on the four non-Overview sections.
Moved, not rebuilt: New Trial / New Class stay one navigation away (the existing
`create-show/wizard?mode=…` deep links), per _links, not re-implementations_.

## Open questions for Richard

1. **Eight or five?** Items 5–8 are setup/lifecycle, 1–4 are daily work. Split the menu with a separator, or cut 5–8 to a nested "Show settings" submenu?
2. **Does the publish row stay on Overview?** Removing it from the other four sections is uncontroversial; removing it from Overview too would make the menu its only home.
3. **New Trial / New Class:** move them into the menu, or leave them on their tabs and accept the duplication?
4. **Setup:** re-add it to the nav (the sidebar still links there), or redirect `/shows/:id/setup` → `/shows/:id`?
5. **"All registrations 514" vs 517** — is that the entry-status bucketing behaving as designed, or a relabel ("All _pending_ registrations")?
6. **Finding #3** (deep-link `?tab=my-entries` fails) — file separately, or fold into MYK9-630's AC 3 since that tab is being rewritten anyway?

## Decisions (Richard, 2026-09-17)

1. Five menu items plus a separator and "Show settings" (edit, copy link, preview, delete live on the setup page, not in the menu).
2. Publish row stays on Overview only.
3. New Trial / New Class stay on their tabs.
4. `/shows/:id/setup` redirects to `/shows/:id`; sidebar link fixed.
5. "All registrations" must mean all (MYK9-635).
6. Deep-link failure filed as MYK9-634.

Mockup of the result: https://claude.ai/artifact/NsYZhBFSHNm9qZnDfqa3xU (secretary "after" board).

## Phase 2 — one row of six tabs

Richard, 2026-09-17: "we still need to simplify all the tabs for the secretary. Seems like there are 11 or more and difficult to tell if they are tabs or links or buttons." Today the show page carries two rows built separately: five page links (Show Desk, Entry Management, Reports, Results, Submit Results) above six tabs (Overview, Show Map, Trials, Classes, Entries, Results). Results is in both rows, the Entries tab is a stub pointing at Entry Management, Submit Results is a step inside Results, and links and tabs are styled alike.

Target: **one row, six tabs, each a real page**, in this order.

| Tab      | Absorbs                                                                                                                                                                                                                                    |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Overview | Overview, publish state, premium                                                                                                                                                                                                           |
| Setup    | Trials, Classes, Show Map                                                                                                                                                                                                                  |
| Entries  | Entry Management itself (the stub tab and its two buttons are deleted)                                                                                                                                                                     |
| Show Day | Show Desk                                                                                                                                                                                                                                  |
| Results  | Results, Submit Results                                                                                                                                                                                                                    |
| Reports  | Reports, grouped by when a secretary uses them: **before** (armband labels, check-in sheets, score sheets), **during** (preliminary results, result labels), **after** (organization reports: secretary and trial reports to AKC/UKC/ASCA) |

Reports is its own tab because it is used in all three phases (Richard), so it does not belong under Results.

Rules: tabs are the only horizontal row on the page; buttons are filled and carry verbs (the Actions button from Phase 1); links are inline text inside content; nothing is an orange word floating in a row. Every old page URL (`/show-desk`, `/entry-management`, `/reports`, `/results`, `/submit-results`, `/setup`) redirects into its tab so bookmarks and the sidebar keep working. Per-tab badges keep their current sources; the Entries badge and body read one source (Phase 1 AC3).

Acceptance for Phase 2: the six tabs render for a manager; the five-link row is gone; each old URL redirects and lands on the right tab; Reports shows the before/during/after grouping; a render test asserts exactly six tab triggers and zero standalone page links in the header region, red on main; staging walk as `secretary@myk9t.com` across all six tabs at desktop and 375px.

## Cross-cutting decision — the Actions button lives in the app header

Richard, 2026-09-17: "one place to go… always visible… if an option is not relevant it would be grayed out."

- One shared `HeaderActions` component in the app header, left of the notifications bell, the same spot on every page and at 375px. Not a per-page button; page headers lose their scattered buttons.
- Contents are a per-page action list resolved from route context (show, dog, entry, role) through one registry. Off a context page the list is the role-wide actions only.
- Greyed with a one-line reason (hover/focus) when an item belongs to the page but is unavailable; absent when it does not belong. Five to seven items per page.
- The command palette reads the same registry, so the two never disagree.
- The sidebar stays navigation; the bottom action-bar registry stays for one primary action on long pages.
- Build order: registry + header component + the secretary list here (MYK9-630); MYK9-631 adds the exhibitor list.

Mockup: https://claude.ai/artifact/NsYZhBFSHNm9qZnDfqa3xU (boards draw the button in the page header; final placement is the app header).

### Placement rule (Richard, 2026-09-17)

An action lives in the header Actions menu when its target is the whole page (the show, this exhibitor's entries at this show). It stays beside the content when the user must first pick which item it applies to (this class row, this entry row, this dog). The same verb never appears in both places. A status banner with a call to action (money due, waiting on payment) keeps its own button and that verb is NOT repeated in the menu. Consequence for MYK9-631: "Pay" stays on the banner and leaves the exhibitor menu.

Cost of the header placement, found in the phase 1 review: the button squeezes the brand wordmark at 360 to 414px, so below the `sm` breakpoint the trigger is icon-only with a screen-reader label, and the wordmark guard e2e signs in as a secretary on a show route, not only as an exhibitor on a page with no actions.
