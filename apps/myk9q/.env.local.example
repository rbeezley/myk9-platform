# myK9Show - Supabase Configuration
# 
# INSTRUCTIONS:
# 1. Copy this file to .env.local
# 2. Fill in your actual Supabase values from your dashboard
# 3. Restart the development server (npm run dev)
#
# DO NOT commit .env.local to git (it's in .gitignore)

# Your Supabase project URL
# Found at: https://app.supabase.com/project/[your-project]/settings/api
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anon/public key  
# Found at: https://app.supabase.com/project/[your-project]/settings/api
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Optional: Environment identifier
VITE_ENVIRONMENT=development

# Push Notifications Configuration
# Enable/disable push notifications
VITE_PUSH_NOTIFICATIONS_ENABLED=true

# VAPID Public Key (safe to expose to client)
# Generate using: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key

# VAPID Private Key (NEVER expose to client - server use only)
# Keep this secure and only use on your server
VAPID_PRIVATE_KEY=your-vapid-private-key

# Push notification server endpoint
# Where push subscriptions will be sent and managed
VITE_PUSH_SERVER_ENDPOINT=http://localhost:3001/api/push

# Email for VAPID configuration (required for web-push)
VAPID_EMAIL=mailto:your-email@domain.com