# Go-Live Gate Review — 2026-07-11

> **Status:** Active

Full launch-checklist pass. Evidence gathered live from the Supabase project (`sojmvhhwsjxmfistvzbe`), the repo, and today's full security audit ([../security-audit-2026-07-11.md](../security-audit-2026-07-11.md)). Statuses: **done** (verified with evidence), **blocked** (defect found, needs a fix), **owner-action** (only Richard can do/verify it).

## 1. Payments (operator-gated)

| Item | Status | Evidence |
| --- | --- | --- |
| Stripe live mode + live keys as edge secrets | **owner-action** | Migration `20260706013906_stripe_livemode_scoped_ids` shipped livemode-scoped IDs; key swap is a Stripe-dashboard + `supabase secrets set` operation only the operator can do. |
| Connect onboarding live; payout schedule Manual | **owner-action** | Manual payout schedule confirmed as the design (memory: Stripe Connect rollout, Phase 6 E2E complete 2026-06-10). Needs one live-mode onboarding walkthrough. |
| Refund path re-verified in live mode | **owner-action** | Refund architecture (platform-balance, capped single refund) shipped and test-mode verified (MP-01..14 closed 2026-07-09, #1240). Live-mode re-verification requires a real charge. |
| Webhook endpoints re-registered for live mode | **owner-action** | `stripe-webhook` supports dual signing secrets (verified in today's audit), so a zero-downtime live cutover is ready — but registration itself is a dashboard action. |

## 2. Email & auth

| Item | Status | Evidence |
| --- | --- | --- |
| Custom SMTP in Supabase Auth | **owner-action — longest lead time** | Not verifiable via MCP (GoTrue config). Until configured, auth emails cap at ~2/hr (memory: auth-email-rate-limit runbook). **Hard launch blocker.** |
| Confirmation-email flow vs real mailbox; AKC recipient gate | **owner-action** | `send-confirmation-email` fail-closed authz verified in today's audit; the real-mailbox + AKC recipient check is a known manual gate (secretary verification sweep, #1242). |
| E2e/demo accounts excluded from prod data/comms | **blocked (decision needed)** | Live DB: 16 of 26 people are `*@myk9t.com` / `*@test.myk9.com` test identities; 5 live shows are demo-seeded. See §4 demo-data decision. |

## 3. Security sweep

| Item | Status | Evidence |
| --- | --- | --- |
| `security-audit` full mode | **done** | Run today: 0 critical, 0 high, 1 medium (SA-021 FORCE RLS on 5 tables), 6 low. [Report](../security-audit-2026-07-11.md). 4 findings auto-fixable. |
| `get_advisors` clean or dispositioned | **blocked (disposition needed)** | Live run 2026-07-11: 2 ERROR (`view_public_entry_results`, `view_authenticated_entry_results` are SECURITY DEFINER views — likely intentional per the results-release-gate design, needs explicit disposition), 3 INFO `rls_enabled_no_policy` (`login_attempts`, `show_money_locks`, `show_passcodes` — service-role-only tables, likely fine), 359 WARN dominated by `anon/authenticated_security_definer_function_executable` (231) — anon-executable SECURITY DEFINER RPCs like `assign_armband`, `create_dog_with_registrations`, `approve_role_request` each check authz internally per the audits, but a one-pass EXECUTE-revoke sweep for anon would disposition the whole class. Also 16 `function_search_path_mutable` (incl. `custom_access_token_hook`) and 2 `public_bucket_allows_listing` (`images`, `premium-published`). |
| Anon surface in cold incognito session | **owner-action** | Manual browser gate (memory: verify-anon-in-cold-session). Not attempted here. |
| Public results release gate | **done** | Migration `20260616120000_public_results_release_gate` applied on remote; owner-run nulling view + column allowlist confirmed in migrations audit. SECURITY DEFINER view advisor hit above is this gate's mechanism — disposition together. |
| Public tables have intentional GRANTs | **done** | `20260613100000_security_revoke_anon_grants` applied; migrations audit found no blanket anon writes; entries INSERT permissiveness carries a RATIONALE comment. |

## 4. Data hygiene

| Item | Status | Evidence |
| --- | --- | --- |
| Demo/seed data fate decided | **owner-action (decision)** | Live DB currently *is* the demo dataset (5 shows, 16 test people). Decide: wipe-and-fresh-start vs. new production project vs. scrub in place. |
| `supabase migration list` parity | **blocked — real defect** | Remote has 360 applied; local has 361 files with a **version collision**: `20260710160000_ringside_passcode_generation_revocation_complete.sql` (applied) and `20260710160000_self_service_soft_delete_person.sql` (**NOT applied** — same version number, so the CLI will never push it and history repair will fight the collision). Fix: rename the unapplied file to a fresh timestamp (e.g. `20260711...`), then `db push`. |
| Crons live with matching Vault secrets | **partial — one defect** | `cron.job`: all 6 jobs active (nightly-show-payouts 02:00, cleanup-ringside-anon 04:00, daily-health-check 07:00, heritage-confirmation-emails 09:00, prune-stale-ringside-sessions 08:17, waitlist-offer-expiration */15m). All runs in the last 36h report `succeeded`. **But** the 2026-07-11 07:00 daily-health-check "succeeded" without writing a `system_health_snapshots` row (latest snapshot is 2026-07-10 07:00, now >30h old) — the DO block swallows a failing pg_net/edge call. The health board will go red at the 26h threshold, and worse, the cron reports success while the probe silently fails. Needs investigation before launch. |

## 5. Product gates

| Item | Status | Evidence |
| --- | --- | --- |
| Feature flags reviewed | **partial** | `shows.unified_ringside_enabled` flag was **removed** (migration `20260623120000_remove_unified_ringside_flag` — checklist item is stale; ringside is now access-gated only). Heritage landing style + premium gating remain data-driven per show/person — reviewing which shows/people carry them is part of the demo-data decision. |
| `/admin/health` green, snapshot < 26h | **blocked** | Latest snapshot 2026-07-10 07:00 UTC (>30h). Same root cause as the cron finding above. |
| PWA prompt mode | **done** | `apps/myk9show/vite.config.ts:55` `registerType: 'prompt'`; `packages/pwa-update/src/sw.ts` wires SKIP_WAITING. |
| Offline ringside smoke test | **owner-action** | Airplane-mode scoring round-trip is a manual device gate (known remaining manual gate from the secretary sweep). |

## 6. Ops readiness

| Item | Status | Evidence |
| --- | --- | --- |
| Sentry alerts routed to a human | **owner-action** | `@sentry/react` integrated (`apps/myk9show/src/services/observability/sentry.ts`, with PII scrubbing verified by tests). Alert *routing* (email/Slack rules in Sentry) can't be verified from the repo. |
| `incident-triage` skill current | **done** | Skill exists in `.claude/skills/` and references current infra (health board, Supabase, ringside OCC patterns). |
| Domain/DNS + Vercel production project | **owner-action** | Staging confirmed (myk9-platform-myk9show.vercel.app, auto-deploy from main). Production domain (myk9show.com) cutover is a Vercel/DNS operator action. |
| Legal: TOS/privacy attorney review | **owner-action** | Docs shipped (mig 121, signup gate, 39 tests); attorney review acknowledged pending (memory: legal-content). |

## Blocking launch — ordered by lead time

1. **Custom SMTP for auth emails** (owner; external setup + DNS records; days) — hard blocker, ~2/hr auth-email cap until done.
2. **Stripe live-mode cutover** (owner; live keys, webhook re-registration, one live-mode E2E incl. refund; days) — operator-gated by design.
3. **TOS/privacy attorney review** (owner; external party; days–weeks, can overlap everything else).
4. **Demo/seed data decision** (owner decision, then ~hours of execution) — 16/26 people and all 5 shows are test data in the DB that would become production.
5. **Health-check probe silently failing** (fixable now; hours) — cron says `succeeded` while no snapshot lands; the monitoring you'd rely on at launch is currently blind *and* not alerting.
6. **Migration version collision** (fixable now; minutes) — `20260710160000_self_service_soft_delete_person.sql` unapplied and unpushable until renamed.
7. **Advisor disposition sweep** (fixable now; ~half day) — decide/document the 2 SECURITY DEFINER views, revoke anon EXECUTE on internal RPCs as a class, pin the 16 mutable search_paths, drop public-bucket listing policies.
8. **Security-audit auto-fixes** (fixable now; ~1 hour) — SA-021 FORCE RLS migration + 3 low mechanical fixes.
9. **Manual device/browser gates** (owner; ~1 hour together) — cold-session anon walk, offline ringside round-trip, real-mailbox confirmation email + AKC recipient.

Items 5–8 are agent-workable today; 1–4 and 9 are yours.
