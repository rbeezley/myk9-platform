/**
 * One-Handed Mode Hook (DEPRECATED)
 *
 * This feature has been removed for simplicity.
 * Hook remains for backward compatibility but does nothing.
 */

/**
 * Hook to apply one-handed mode classes to the document (NO-OP)
 *
 * This feature has been removed to simplify the UI for non-technical users.
 * The hook remains to prevent breaking changes in components that use it.
 */
export function useOneHandedMode() {
  // No-op: One-Handed Mode feature has been removed
}
