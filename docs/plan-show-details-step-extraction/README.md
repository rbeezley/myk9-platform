# Show Details Step Extraction

> **Status:** Active

Generated by the `/improve` skill (plan-only invocation) against commit
`fb144d4d1` on 2026-06-30, following up on a `/hotspots` churn×size scan that
flagged `ShowDetailsStep.tsx` as the highest-leverage untouched hotspot (486
LOC, 47 commits / 6 months, no prior dedicated extraction — unlike its sibling
`ShowDetailsPage.tsx`, already split across PRs #1032–#1036).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Extract Basic Show Info section and mutation handlers out of ShowDetailsStep.tsx | P2 | M | — | TODO (re-dispatched after BLOCKED run 1) |

## Execution log

- **Run 1 (2026-06-30)**: BLOCKED. Executor correctly hit Step 1's STOP condition before any edit — `wizardStore.ts`'s `WizardState` interface is unexported and `show` has no nameable type, so `BasicShowInfoSectionProps.show: ShowDraft` couldn't be satisfied without inventing a type, which the plan explicitly forbade. No files changed; clean stop. Plan patched: added Step 0 (export `type ShowDraft = WizardState['show']` in `wizardStore.ts`, exactly one line) and added `wizardStore.ts` to in-scope with a tight STOP guard against any other change in that file. Re-dispatched as run 2.
- **Run 2 (2026-06-30)**: REVISE (round 1). Steps 0-5 all completed and verified individually (typecheck, lint, both targeted vitest runs, line count 206, no stray handler definitions, scope clean) and committed to branch `refactor/show-details-step-extraction` at `2ba4fd8b9` / `aa43c108c` / `8cc1e824b`. The repo-wide `pnpm test` final gate caught one out-of-scope casualty the plan didn't anticipate: `apps/myk9show/src/test/components/wizard/wizardThemingA11y.test.ts:92`, a pinned-string regression test that read `ShowDetailsStep.tsx` directly and asserted on `text-sm text-destructive mt-1` — that string correctly moved to `ShowDetailsStep.sections.tsx` in Step 1/2, and the test wasn't told. Executor stopped rather than improvise an out-of-scope edit — correct behavior. Verified the diagnosis directly (string present at `ShowDetailsStep.sections.tsx:306,327`, absent from `ShowDetailsStep.tsx`) and confirmed the same file already has an identical precedent (`validationBanner`, lines 21-28) for "markup moved → add a new `read()`, repoint the assertion." Patched the plan with Step 6 (one new `const`, one changed assertion line) and added the test file to scope. Resumed the same executor (not a fresh dispatch) via SendMessage to apply Step 6 and re-run the final gate.

## Dependency notes

None — single plan.

## Findings considered and rejected

- **Duplication between `ShowDetailsStep.tsx` and `ShowDetailsPage.tsx`**: investigated per CLAUDE.md's "consolidate, don't duplicate" rule and rejected — they are different concerns (pre-creation wizard step vs. post-creation show viewer/manager) with no shared logic. Not re-investigating unless a future PR adds real overlap.
- **`ShowDetailsStep.tsx` LOC-shaving via the 500-line ratchet alone**: the file is 486 LOC, under the 500-line ratchet — it is not currently failing CI. Plan 001 is justified by maintainability (mixed concerns: store-derived state, ~185 lines of pure markup, ~70 lines of mutation handlers all in one component) and by matching the file's own existing extraction pattern (it already has `ShowDetailsStep.sections.tsx`, `.helpers.ts`, `.types.ts`, `.FeeField.tsx`), not by the ratchet number alone.

## Close-out

When plan 001 merges: flip this file's `> **Status:**` to `Complete`, `git mv`
this directory to `docs/archive/plan-show-details-step-extraction/`, and
remove its row from `docs/README.md` — per the repo's anti-rot convention
(see `docs/README.md` § "Anti-rot convention").
